/**
 * Retry policy (KTD-2, R9).
 *
 * **Retries are GET-only.** The API has no `Idempotency-Key` support, and
 * `POST /api/v1/jobs` twice charges two credits — so a retried write is a
 * double charge, a duplicate schedule, or a duplicate workflow. No timeout
 * heuristic makes that safe, so POST/PATCH/PUT/DELETE are never retried at all,
 * not even on a network error where the request may never have landed.
 *
 * **429 IS retried.** The API returns 429 on the BI query mount
 * (`too_many_queries`), on the daily URL cap, and on extraction — where it also
 * sets `Retry-After`. Not every 429 carries that header, so the header is
 * honored when present and its absence is never an error.
 *
 * **402 is never retried.** Retrying an exhausted quota cannot succeed and just
 * spends the customer's time; it needs a human to add credits.
 */
export interface RetryPolicy {
	/** Attempts *after* the first. `0` disables retrying. */
	maxRetries: number;
	/** First backoff step, doubled per attempt. */
	baseDelayMs: number;
	/** Ceiling on any single backoff, including one derived from `Retry-After`. */
	maxDelayMs: number;
}
export declare const DEFAULT_RETRY_POLICY: RetryPolicy;
/**
 * Whether a *response* status should be retried for this method.
 *
 * @param method  HTTP method, any casing.
 * @param status  Response status.
 */
export declare function isRetryableResponse(method: string, status: number): boolean;
/**
 * Whether a *transport* failure (no response at all) should be retried.
 *
 * Same method restriction: a POST that failed mid-flight may still have been
 * processed, and the SDK has no way to find out.
 */
export declare function isRetryableTransportError(method: string): boolean;
/**
 * Parse a `Retry-After` header value into seconds.
 *
 * Handles both RFC 7231 forms — delta-seconds and an HTTP-date — and returns
 * `undefined` for anything unparseable rather than throwing. A past date
 * clamps to 0, never a negative delay.
 */
export declare function parseRetryAfter(value: string | null | undefined, now?: number): number | undefined;
/**
 * GENERATED FILE — DO NOT EDIT.
 *
 * Produced by `openapi-typescript` from `apps/docs/src/openapi.yaml`.
 * Regenerate with:  pnpm exec tsx tooling/scripts/verify-sdk-types.ts --write
 *
 * The committed spec has zero `operationId` values, so the `operations`
 * export is empty by construction: reference types through
 * `paths['/api/v1/…'][method]['responses'][200]['content']['application/json']`
 * instead. See libraries/sdk/sdk-js/CLAUDE.md.
 */
/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */
export interface paths {
	"/api/v1/account": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		post?: never;
		/** Delete your account (GDPR right to erasure) */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description Email confirmation (must match account email) */
						email: string;
					};
				};
			};
			responses: {
				/** @description Account deleted successfully */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Email confirmation does not match */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description User not found */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Account cannot be deleted (e.g. owns active org) */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/account/export": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Export all personal data (GDPR right to data portability) */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description JSON file download containing user, org membership, workflows, jobs, schedules, API keys, settings, and audit logs */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": Record<string, never>;
					};
				};
				/** @description User not found */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/analytics/overview": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get overview metrics
		 * @description Headline metrics for the dashboard's overview card — total requests, success rate,
		 *     p50/p95 latency, total spend, freshness timestamp. Org-scoped.
		 *
		 *     This is a **cached read** populated by the analytics service on its own cadence
		 *     (typically every 1–5 minutes). When the cache is cold (new org, never-rolled window,
		 *     or post-Redis-restart), the endpoint returns a zeroed object rather than 404 so the
		 *     dashboard renders empty state without a second probe. Use `latestDataAt` to tell
		 *     "no activity" from "cache not ready."
		 */
		get: {
			parameters: {
				query?: {
					/** @description Lookback window in hours (default 24, max 720 = 30 days). Out-of-range values clamp to the default rather than 400. */
					hours?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Overview metrics for the organization (zeroed object when cache is cold). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							totalRequests?: number;
							successCount?: number;
							successRate?: number;
							avgLatencyMs?: number;
							p95LatencyMs?: number;
							totalCost?: number;
							/** Format: date-time */
							latestDataAt?: string | null;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/analytics/timeseries": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get timeseries data
		 * @description Bucketed time-series points for the dashboard chart — request count, success rate,
		 *     and latency per bucket. Org-scoped, cached read.
		 *
		 *     The `interval` enum (`5m` / `15m` / `30m` / `1h` / `6h` / `1d`) is the bucket width.
		 *     Unsupported values return 400 — the bucketed cache only exists for these widths, by
		 *     design (the analytics service pre-computes them on a fixed schedule). Empty array when
		 *     the cache is cold.
		 *
		 *     **Cache coverage** — the analytics service pre-populates only these specific
		 *     `(hours, interval)` pairs (defined in `services/analytics/src/cache-pusher.ts`,
		 *     constant `TIMESERIES_CONFIGS`) — `(1, 5m)`, `(6, 15m)`, `(24, 30m)`, `(168, 6h)`,
		 *     `(720, 1d)`. Other combinations are accepted by the schema but return an empty array.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Lookback window in hours. Cache-populated values are 1, 6, 24, 168, and 720 — each paired with a specific `interval` per the description above. */
					hours?: number;
					/** @description Bucket width (default `1h`). Must be the interval paired with `hours` in `TIMESERIES_CONFIGS` to return data. */
					interval?: "5m" | "15m" | "30m" | "1h" | "6h" | "1d";
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Array of bucketed timeseries points. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": Record<string, never>[];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/analytics/providers": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Provider comparison
		 * @description Per-provider rollup over the window — request count, success rate, p50/p95 latency,
		 *     cost per provider. Org-scoped, cached read. Only providers this org has actually
		 *     routed traffic to during the window appear; idle providers are omitted entirely rather
		 *     than reported as zero rows.
		 *
		 *     **Cache coverage:** the analytics service currently pre-populates only the `hours=24`
		 *     key (see `services/analytics/src/cache-pusher.ts`). Passing other `hours` values
		 *     returns an empty array — deliberate caching budget decision, not a missing-data signal.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Lookback window in hours (default 24, max 720). Only `hours=24` is cache-populated today; other values clamp valid but return empty. */
					hours?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Per-provider rollup (one row per provider the org has used in the window). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": Record<string, never>[];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/analytics/domains": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Domain breakdown
		 * @description Top domains by request volume over the window, plus per-domain success rate, latency,
		 *     and cost. Org-scoped, cached read. Sorted by request count desc; capped at `limit`
		 *     rows.
		 *
		 *     **Cache coverage:** the analytics service currently pre-populates only the
		 *     `(hours=24, limit=20)` key (see `services/analytics/src/cache-pusher.ts`). Other
		 *     `(hours, limit)` combinations return an empty array — deliberate caching budget
		 *     decision, not a missing-data signal.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Lookback window in hours (default 24, max 720). Only `hours=24` is cache-populated today. */
					hours?: number;
					/** @description Top-N cap (default 20, max 100). Only `limit=20` (at `hours=24`) is cache-populated today. */
					limit?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Top-N domains by request volume. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": Record<string, never>[];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/analytics/matrix": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Domain x provider matrix
		 * @description Domain × provider cross-tab — for each `(domain, provider)` cell the org has actually
		 *     used during the window, the request count, success rate, latency, and cost. Powers the
		 *     dashboard's heatmap. Org-scoped, cached read. Cells with zero traffic are omitted (the
		 *     heatmap renders missing cells as empty); empty array when the cache is cold.
		 *
		 *     **Cache coverage:** the analytics service currently pre-populates only the `hours=24`
		 *     key (see `services/analytics/src/cache-pusher.ts`). Other `hours` values return an
		 *     empty array — deliberate caching budget decision, not a missing-data signal.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Lookback window in hours (default 24, max 720). Only `hours=24` is cache-populated today. */
					hours?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Array of `(domain, provider)` cells with rollup metrics. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": Record<string, never>[];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/audit-logs": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** List audit logs for the organization */
		get: {
			parameters: {
				query?: {
					page?: number;
					limit?: number;
					action?: string;
					startDate?: string;
					endDate?: string;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated audit log entries */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Invalid startDate or endDate */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Missing org:manage permission */
				403: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/register": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Register a new account
		 * @description Creates a new user account and signs them in immediately — the response
		 *     includes a 15-minute `accessToken` and a 7-day `refreshToken` so the
		 *     client can start making authenticated requests without a second round-trip.
		 *     Email verification is required for write-heavy operations (CSV upload,
		 *     schedule create / trigger) but the account is usable read-only until
		 *     verified; a verification email is sent automatically. The `agreedToTerms`
		 *     field is binding — the API refuses to create an account without it, and
		 *     the timestamp + terms version are persisted to the user row. Subject to
		 *     the auth rate-limit bucket — a flurry of signups from one IP returns 429.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** Format: email */
						email: string;
						/** @description 8-72 characters. Bcrypt-hashed at rest with cost factor 12. */
						password: string;
						/** @description Display name. Shown in the dashboard and in outbound emails. */
						name: string;
						/** @description Must be `true`. The server records the agreement timestamp + current terms version against the user row. */
						agreedToTerms: boolean;
					};
				};
			};
			responses: {
				/** @description Account created. Body carries the user identity, a 15-min `accessToken`, and a 7-day `refreshToken`. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							user?: {
								/** Format: uuid */
								id?: string;
								/** Format: email */
								email?: string;
								name?: string;
								/** @description Always null on register; set when the verification email is clicked. */
								emailVerifiedAt?: string | null;
							};
							accessToken?: string;
							refreshToken?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				409: components["responses"]["Conflict"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/login": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Log in with email and password
		 * @description Exchanges credentials for a 15-minute `accessToken` + 7-day `refreshToken`.
		 *     The 401 response is identical for both "no such email" and "wrong password"
		 *     — the server never discloses which side failed, blocking email enumeration.
		 *     The response embeds the user's resolved `orgId`, `orgName`, and effective
		 *     `permissions` (RBAC) so a freshly-logged-in client can render the
		 *     dashboard without a follow-up org-lookup roundtrip. Subject to the
		 *     auth rate-limit bucket — many failed logins from one IP return 429.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** Format: email */
						email: string;
						password: string;
					};
				};
			};
			responses: {
				/** @description Authenticated. Body carries user identity + org context + tokens. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							user?: {
								/** Format: uuid */
								id?: string;
								/** Format: email */
								email?: string;
								name?: string;
								emailVerifiedAt?: string | null;
								/** @description Null when the user hasn't completed onboarding. */
								orgId?: string | null;
								orgName?: string | null;
							};
							accessToken?: string;
							refreshToken?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/refresh": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Refresh access token using a refresh token
		 * @description Exchanges a valid refresh token for a new access/refresh token pair.
		 *     Refresh tokens are rotated on every use — the old token is invalidated
		 *     atomically before the new one is issued, so accidentally reusing a
		 *     prior refresh token returns 401. Refresh tokens live 7 days; access
		 *     tokens live 15 minutes. Subject to a dedicated `refreshLimiter` rate
		 *     bucket sized for normal refresh cadence — a tight loop hitting this
		 *     endpoint returns 429.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						refreshToken: string;
					};
				};
			};
			responses: {
				/** @description New access and refresh tokens issued. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @description Echoed for client-side hydration after token rotation. */
							user?: Record<string, never> | null;
							accessToken?: string;
							refreshToken?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/logout": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Invalidate a refresh token
		 * @description Revokes the supplied refresh token server-side so it can no longer be
		 *     exchanged for new access tokens. The access token itself is unaffected —
		 *     short-lived (15 minutes) and stateless, so the client should drop it
		 *     locally as part of the same logout flow. Calling without a `refreshToken`
		 *     still returns 204; the endpoint is intentionally tolerant so client
		 *     logout buttons can fire blindly without first re-reading storage.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: {
				content: {
					"application/json": {
						refreshToken?: string;
					};
				};
			};
			responses: {
				/** @description Refresh token revoked (or no-op if missing). No response body. */
				204: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/forgot-password": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Request a password reset link
		 * @description Initiates the password-reset flow. The response is always 200 with a
		 *     generic message — the server never discloses whether the email maps to
		 *     an account, blocking email enumeration. When the email does map to an
		 *     account, a single-use reset token is emailed with a 1-hour TTL, and any
		 *     previously-issued reset tokens for the same user are invalidated.
		 *     Subject to a dedicated `forgotPasswordLimiter` rate bucket — bursts of
		 *     requests for the same email return 429.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** Format: email */
						email: string;
					};
				};
			};
			responses: {
				/** @description Reset email sent if the account exists. Body is identical for both branches by design. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @example If that email exists, a reset link has been sent */
							message?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/reset-password": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Reset password using a reset token
		 * @description Completes the password-reset flow by exchanging the reset token (emailed
		 *     by `POST /api/v1/auth/forgot-password`) for a new password. Side effects:
		 *     the reset token is marked used (single-use); ALL of the user's refresh
		 *     tokens are revoked so existing sessions on every device are forced to
		 *     re-login. The client must call `POST /api/v1/auth/login` after success
		 *     to obtain a new access/refresh token pair.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						token: string;
						password: string;
					};
				};
			};
			responses: {
				/** @description Password reset successful. All refresh tokens revoked across all devices. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @example Password has been reset */
							message?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/verify-email": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Verify email address using a verification token
		 * @description Completes the email-verification flow. The token is single-use and has
		 *     a 24-hour TTL. On success the user's `emailVerifiedAt` timestamp is set
		 *     and all pending verification tokens for this user are invalidated. The
		 *     endpoint is idempotent: calling with a valid token on an already-verified
		 *     account returns 200 with the "already verified" message instead of an
		 *     error. Email verification unlocks the write-heavy operations (CSV
		 *     upload, schedule create / trigger).
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						token: string;
					};
				};
			};
			responses: {
				/** @description Email verified (or already verified). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							message?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/resend-verification": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Resend email verification token (requires authentication)
		 * @description Reissues a verification email for the authenticated user. Any
		 *     previously-outstanding verification tokens for this user are invalidated
		 *     before the new one is created, so only the most recent email link works.
		 *     400 if the user is already verified — there's no harmless re-send path;
		 *     the client should branch on `emailVerifiedAt` from `/api/v1/auth/login`
		 *     before calling. Pass `context: "onboarding"` to receive a verification
		 *     link that redirects into the onboarding wizard after clicking, rather
		 *     than the dashboard home.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: {
				content: {
					"application/json": {
						/**
						 * @description Optional redirect context for the verification link.
						 * @enum {string}
						 */
						context?: "onboarding";
					};
				};
			};
			responses: {
				/** @description Verification email sent. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							message?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				/** @description User not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/auth/me": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get the authenticated user
		 * @description Returns the user behind the presented credential. Use it to confirm an API
		 *     key or access token is still valid and to read the current email-verification
		 *     state without a re-login. The `user` shape matches the one returned by
		 *     `POST /api/v1/auth/login`.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description The authenticated user. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							user?: {
								/** Format: uuid */
								id?: string;
								/** Format: email */
								email?: string;
								name?: string | null;
								/**
								 * Format: date-time
								 * @description Null until the user verifies their email address.
								 */
								emailVerifiedAt?: string | null;
							};
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				/**
				 * @description The token authenticated, but its subject no longer exists — a deleted
				 *     account whose access token has not yet expired.
				 */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/benchmarks": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Start benchmark
		 * @description Kicks off a benchmark run — every connected provider scrapes the supplied URLs in
		 *     parallel so the platform can rank them on success rate, latency, and cost. Returns
		 *     `201` with a `pending` benchmark record immediately; the actual scraping happens
		 *     async (`setImmediate` → `runBenchmark`). Poll `GET /benchmarks/{id}` for status.
		 *
		 *     - **Hard cap:** 20 URLs per benchmark. Larger sets return 400.
		 *     - **Auto-detect:** providers are taken from this org's connected accounts; orgs with
		 *       zero connected providers get 400 with an actionable message.
		 *     - **One-at-a-time per org:** a second POST while one is `pending` or `running` returns
		 *       409. Invalid URLs are silently skipped; if every URL is invalid the call 400s.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						urls: string[];
					};
				};
			};
			responses: {
				/** @description Benchmark queued; scraping runs async. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Benchmark"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				409: components["responses"]["Conflict"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/benchmarks/latest": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get latest benchmark
		 * @description Returns the most recent benchmark for this org, of any status (`pending` / `running`
		 *     / `completed` / `failed`). Useful for the dashboard's "Resume where you left off"
		 *     prompt. Returns 404 when the org has never run a benchmark.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Latest benchmark for the organization. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Benchmark"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/benchmarks/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get benchmark status
		 * @description Fetches a single benchmark's status row — designed for polling while a benchmark is
		 *     `pending` or `running`. The `progress` field tracks completed probes vs total.
		 *     Org-scoped; cross-tenant probes return 404 indistinguishably from missing IDs.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Benchmark details + progress. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Benchmark"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/benchmarks/{id}/cancel": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Cancel benchmark
		 * @description Cancels a `pending` or `running` benchmark by transitioning it to `failed` with the
		 *     reason "Cancelled by user." Cancellation is cooperative — the in-flight runner exits
		 *     at its next checkpoint, so already-dispatched probes may still bill before the row
		 *     flips. `completed` and already-`failed` benchmarks return 400 (nothing to cancel).
		 *     Org-scoped — cross-tenant probes return 404 indistinguishably from missing IDs.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Benchmark cancelled; runner exits at next checkpoint. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {string} */
							status?: "cancelled";
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/benchmarks/{id}/results": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get benchmark results
		 * @description Returns the benchmark's aggregated `(domain, provider)` rollups plus the raw probe
		 *     details for the dashboard's drill-down. Powers the per-domain ranking view used by
		 *     `POST /benchmarks/{id}/generate-workflow`. Org-scoped; cross-tenant probes return 404
		 *     indistinguishably from missing IDs.
		 *
		 *     `skippedProviders` lists providers that were configured but had zero successful probes
		 *     (network errors, provider account misconfigured) — useful for surfacing "Bright Data
		 *     was skipped because its account had no credit" without making the customer dig through
		 *     individual probe logs.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Aggregated benchmark results + per-probe detail. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							benchmarkId?: string;
							status?: string;
							skippedProviders?: string[];
							results?: Record<string, never>[];
							probeDetails?: Record<string, never>[];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/schema": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List queryable tables
		 * @description The tables in your organization's `bi` schema — the entire surface a BI query can read.
		 *     Results are cached for 60 seconds per org, which sits well inside the hourly sync that
		 *     maintains these views, so a newly synced table can take up to a minute to appear here.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Tables visible to this organization */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							tables?: {
								name: string;
								schema?: string;
								description?: string;
								rowCountEstimate?: number;
							}[];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/tables/{name}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Describe one table
		 * @description Column metadata for a single table in your `bi` schema. `sampleValues` carries up to five
		 *     cached example values per column, refreshed daily — treat its absence as "not sampled yet",
		 *     not as "no data". A name that does not resolve for your org returns 404 rather than
		 *     distinguishing "no such table" from "not yours".
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Table name. Must match the identifier pattern; anything else is rejected as `invalid_table_name`. */
					name: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Column metadata */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							name?: string;
							columns?: {
								name: string;
								type: string;
								nullable: boolean;
								description?: string;
								/** @description Up to 5 cached example values. Omitted until the daily sampler has run. */
								sampleValues?: unknown[];
							}[];
						};
					};
				};
				/** @description Table name failed the identifier pattern (`invalid_table_name`) */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "invalid_table_name";
							};
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/queries": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List submitted queries
		 * @description Query history for your organization, newest first. Which engine executed a query is
		 *     deliberately not surfaced — routing is transparent, and freshness and timeout hints come
		 *     from `GET /api/v1/bi/queries/{id}/result-manifest` instead.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Rows to return (default 50, max 200) */
					limit?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Query history */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							queries: components["schemas"]["BiQuery"][];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		/**
		 * Submit a query
		 * @description Submit read-only SQL against your `bi` schema. The call waits briefly for the query to
		 *     finish: if it completes inside that window the first page of rows comes back in the same
		 *     response under `results`; otherwise you get `{ jobId, status }` and poll
		 *     `GET /api/v1/bi/queries/{id}`.
		 *
		 *     **A 200 does not mean the query succeeded.** A query that failed fast returns 200 with
		 *     `status: failed` and an `error` object describing *the query's* failure — that `error` is
		 *     query detail, not an HTTP error envelope, and carries no `docs_url` or `request_id`.
		 *     Branch on `status`, not on the HTTP code.
		 *
		 *     `dryRun` is the pre-submit `EXPLAIN` estimate. It is informational: nothing is rejected
		 *     for size, and only the engine's own time limit is enforced.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description The query. Read-only; max 100000 characters. */
						sql: string;
						/** @description Optional human label shown in query history. Max 200 characters. */
						label?: string;
					};
				};
			};
			responses: {
				/**
				 * @description Query accepted. `results` is present only when the query completed inside the
				 *     sync-wait window; `error` is present only when it failed inside that window.
				 */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							jobId: string;
							/** @enum {string} */
							status: "queued" | "running" | "success" | "failed" | "canceled";
							/** @description Informational EXPLAIN estimate from the Postgres dry-run planning pass. All three fields are present together when the estimate succeeded, and all three are absent (an empty object) when no estimate exists — the CH-force kill switch skips the dry-run entirely, or it timed out before producing a plan. There is no `valid` flag on the wire; the caller cannot distinguish "skipped" from "timed out" from this field alone. */
							dryRun?: {
								planRows?: number;
								planWidth?: number;
								estimatedBytes?: number;
							};
							results?: components["schemas"]["BiQueryResults"];
							/** @description The query's own failure detail — not an HTTP error envelope. */
							error?: {
								/** @enum {string} */
								code?: "syntax_error" | "statement_timeout" | "rls_denied" | "too_many_queries" | "kill_switched" | "canceled" | "transient";
								message?: string;
								retriable?: boolean;
							};
						};
					};
				};
				/** @description Body rejected (`invalid_sql`, `invalid_label`) or the SQL failed validation (`syntax_error`) */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "invalid_sql" | "invalid_label" | "syntax_error";
							};
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				/**
				 * @description The engine could not complete the submission and the failure is not attributable to
				 *     your SQL (`transient`, `canceled`). Retrying is reasonable.
				 */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "transient" | "canceled";
							};
						};
					};
				};
				/**
				 * @description Your organization's concurrency or queue-depth cap is full (`too_many_queries`).
				 *
				 *     **No `Retry-After` header is sent** — this API emits none anywhere on the BI surface,
				 *     so do not read one. Back off with capped exponential retry instead.
				 */
				429: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "too_many_queries";
							};
						};
					};
				};
				500: components["responses"]["BiServerError"];
				/**
				 * @description The query exceeded the engine's time limit (`statement_timeout`). The limit that
				 *     applied is reported as `maxQuerySec` by the result manifest.
				 */
				504: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "statement_timeout";
							};
						};
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/queries/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get query status
		 * @description Current status of one submitted query — the endpoint you poll after a submit that did not
		 *     complete inline. The status object is returned bare, not wrapped. A query belonging to
		 *     another organization returns 404, never 403.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Current status */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							jobId: string;
							/** @enum {string} */
							status: "queued" | "running" | "success" | "failed" | "canceled";
							/** @description Position in your organization's queue. Present only while `status` is `queued`. */
							queuePosition?: number;
							/** Format: date-time */
							startedAt?: string;
							/** Format: date-time */
							finishedAt?: string;
							rowCount?: number;
							/** @description Informational only; not billed and not enforced. */
							bytesScanned?: number;
							error?: {
								/** @enum {string} */
								code?: "syntax_error" | "statement_timeout" | "rls_denied" | "too_many_queries" | "kill_switched" | "canceled" | "transient";
								message?: string;
								retriable?: boolean;
							};
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/queries/{id}/result-manifest": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get result manifest
		 * @description Everything needed to plan pagination and render a freshness hint without fetching a page
		 *     of rows. `freshnessSec` is an upper bound on how stale the underlying data may be and
		 *     `maxQuerySec` is the time limit that applied — both are expressed as data properties
		 *     because which engine ran the query is not part of the contract.
		 *
		 *     `rowCount`, `byteSize`, `completedAt`, and `totalPages` are null until the query reaches
		 *     `success`.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Result manifest */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							manifest: {
								/** @enum {string} */
								status: "queued" | "running" | "success" | "failed" | "canceled";
								rowCount?: number | null;
								byteSize?: number | null;
								/** Format: date-time */
								completedAt?: string | null;
								/** @description Upper bound, in seconds, on how far behind live the data may be. */
								freshnessSec: number;
								/** @description Time limit that applied to this query. */
								maxQuerySec: number;
								totalPages?: number | null;
								pageSize: number;
							};
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/queries/{id}/results": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Fetch a page of results
		 * @description One page of result rows for a query that reached `success`.
		 *
		 *     The page object is returned **bare** here. The same object appears **nested under
		 *     `results`** in the `POST /api/v1/bi/queries` response — the two are not interchangeable,
		 *     and a client that unwraps one shape for the other silently reads `undefined`.
		 *
		 *     Polling a query that has not finished returns 409 `not_ready`, whose body carries the
		 *     current `status` as a sibling of `error`.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Zero-based page index (default 0) */
					page?: number;
					/** @description Rows per page (default 200, max 1000) */
					pageSize?: number;
				};
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description One page of rows */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiQueryResults"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				/**
				 * @description The query has not reached `success` (`not_ready`), or the engine failed to serve the
				 *     page for a reason unrelated to your SQL (`transient`, `canceled`). Only `not_ready`
				 *     carries the sibling `status` field.
				 */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "not_ready" | "transient" | "canceled";
							};
							/**
							 * @description Current query status, alongside `error` rather than inside it. Present on `not_ready` only.
							 * @enum {string}
							 */
							status?: "queued" | "running" | "failed" | "canceled";
						};
					};
				};
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/queries/{id}/cancel": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Cancel a running query
		 * @description Best-effort cancellation. Idempotent: a second cancel on an already-finished query returns
		 *     cleanly rather than erroring, so a client that retries after a timeout does not need to
		 *     reason about which call won. A `200` means the cancel was accepted, not that the query has
		 *     already stopped — poll `GET /api/v1/bi/queries/{id}` to observe the terminal status.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Cancel accepted */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							ok: boolean;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				500: components["responses"]["BiServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/saved-queries": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List saved queries
		 * @description Your organization's saved queries, most recently updated first.
		 *
		 *     **List rows omit `shareToken` by design.** A share token is a live bearer capability —
		 *     anyone holding one can read that query's results with no credential — so returning every
		 *     token in an org from one unauthenticated-by-default surface would make bulk disclosure the
		 *     default. Rows carry `isShared` instead; fetch the single query when you actually need the
		 *     link.
		 *
		 *     `scheduleCount` is how many schedules are attached, so a client can warn before deleting
		 *     without a per-row round-trip.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Saved queries */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							savedQueries: (components["schemas"]["BiSavedQuery"] & {
								/** @description Whether a share token exists. The token itself is never returned here. */
								isShared: boolean;
								/** @description Number of schedules attached to this saved query. */
								scheduleCount: number;
							})[];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		/**
		 * Create a saved query
		 * @description Persist a query under a name. Names are unique per organization; a collision returns 409
		 *     `saved_query_name_taken`. A malformed `chartConfig` returns 422 rather than 400, so a
		 *     broken chart picker payload is distinguishable from a missing `name` or `sql`.
		 *
		 *     Saving does not run anything, and saved queries take no parameters — the stored `sql` is
		 *     literal text you submit yourself via `POST /api/v1/bi/queries`.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description Unique within the organization. Max 120 characters. */
						name: string;
						/** @description Max 100000 characters. */
						sql: string;
						/** @description Max 1000 characters. */
						description?: string | null;
						chartConfig?: components["schemas"]["ChartConfig"] | null;
					};
				};
			};
			responses: {
				/** @description Created */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							savedQuery: components["schemas"]["BiSavedQuery"];
						};
					};
				};
				/** @description Body rejected (`invalid_name`, `invalid_sql`, `invalid_description`) */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "invalid_name" | "invalid_sql" | "invalid_description";
							};
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				/** @description A saved query with that name already exists (`saved_query_name_taken`) */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "saved_query_name_taken";
							};
						};
					};
				};
				/** @description The `chartConfig` field is present but malformed (`invalid_chart_config`) */
				422: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"];
					};
				};
				500: components["responses"]["BiServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/saved-queries/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get a saved query
		 * @description One saved query, including its `shareToken` when a share link has been minted. That token
		 *     grants credential-free read access to the query's results — treat the response as
		 *     containing a secret, and do not log or forward it.
		 *
		 *     There is no endpoint that runs a saved query. To execute one, read its `sql` here and
		 *     submit it to `POST /api/v1/bi/queries`.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description The saved query */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							savedQuery: components["schemas"]["BiSavedQuery"];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		post?: never;
		/**
		 * Delete a saved query
		 * @description Permanent delete. 204 on success.
		 *
		 *     Schedules attached to the query decide the outcome, and the two cases differ:
		 *     **enabled** schedules block the delete with 409 `has_active_schedules`, whose `error`
		 *     object carries a `schedules` array of the blocking names so you can tell the user exactly
		 *     what to disable; **disabled** schedules do not block and are removed along with the query.
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Deleted. No body. */
				204: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				/** @description Enabled schedules are attached (`has_active_schedules`) */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							error: {
								/** @enum {string} */
								code: "has_active_schedules";
								message: string;
								docs_url: string;
								request_id: string;
								/** @description Names of the enabled schedules blocking the delete — inside `error`, not beside it. */
								schedules: string[];
							};
						};
					};
				};
				500: components["responses"]["BiServerError"];
			};
		};
		options?: never;
		head?: never;
		/**
		 * Update a saved query
		 * @description Partial update. Any field you omit is left unchanged; at least one must be present or the
		 *     call returns 400 `invalid_body`.
		 *
		 *     `description` and `chartConfig` distinguish omission from clearing: sending `null`
		 *     explicitly clears the field, while leaving the key out keeps the current value.
		 */
		patch: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						name?: string;
						sql?: string;
						/** @description Send `null` to clear; omit to keep. */
						description?: string | null;
						/** @description Send `null` to clear; omit to keep. */
						chartConfig?: components["schemas"]["ChartConfig"] | null;
					};
				};
			};
			responses: {
				/** @description Updated */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							savedQuery: components["schemas"]["BiSavedQuery"];
						};
					};
				};
				/** @description Empty body (`invalid_body`) or a rejected field (`invalid_name`, `invalid_sql`, `invalid_description`) */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "invalid_body" | "invalid_name" | "invalid_sql" | "invalid_description";
							};
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				/** @description The new name is already taken in this organization (`saved_query_name_taken`) */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "saved_query_name_taken";
							};
						};
					};
				};
				/** @description The `chartConfig` field is present but malformed (`invalid_chart_config`) */
				422: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"];
					};
				};
				500: components["responses"]["BiServerError"];
			};
		};
		trace?: never;
	};
	"/api/v1/bi/queries/{id}/exports": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Request a result export
		 * @description Enqueue a CSV or Parquet export of a completed query's results, then poll
		 *     `GET /api/v1/bi/exports/{exportId}` for the download link.
		 *
		 *     **The status code tells you whether work was started.** `202` means a new export was
		 *     enqueued (`reused: false`); `200` means an existing queued, running, or ready export for
		 *     the same query and format was returned instead (`reused: true`). Repeated clicks
		 *     therefore collapse onto one job rather than fanning out. A previously *failed* export is
		 *     never reused — asking again after a failure starts a fresh attempt.
		 *
		 *     Exports are capped at 100000 rows (configurable per deployment); a larger result returns
		 *     400 `result_too_large` with `rowCount` and `maxRowCount` as siblings of `error`. Narrow
		 *     the query with `LIMIT` or `WHERE` and re-run it.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The source query. Must have reached `success`. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @enum {string} */
						format: "csv" | "parquet";
					};
				};
			};
			responses: {
				/** @description An existing export was reused rather than starting a new one */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							export: components["schemas"]["BiQueryExport"];
							reused: boolean;
						};
					};
				};
				/** @description A new export was enqueued */
				202: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							export: components["schemas"]["BiQueryExport"];
							reused: boolean;
						};
					};
				};
				/** @description Unsupported `format` (`invalid_format`), or the result exceeds the export row cap (`result_too_large`) */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "invalid_format" | "result_too_large";
							};
							/** @description Rows in the source result. Present on `result_too_large` only, alongside `error`. */
							rowCount?: number;
							/** @description The export row cap. Present on `result_too_large` only. */
							maxRowCount?: number;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				/** @description The source query has not reached `success` (`not_ready`) */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["BiError"] & {
							error?: {
								/** @enum {string} */
								code?: "not_ready";
							};
							/** @enum {string} */
							status?: "queued" | "running" | "failed" | "canceled";
						};
					};
				};
				500: components["responses"]["BiServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/bi/exports/{exportId}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get export status and download link
		 * @description Poll an export. Once `status` is `ready`, the response carries a freshly signed
		 *     `downloadUrl` valid for `downloadUrlExpiresInSec` seconds.
		 *
		 *     The URL is minted on every poll, so an export picked up hours later still has a live link.
		 *     It grants read access to the export bytes with no credential — fetch it when you need it
		 *     rather than storing or sharing it.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					exportId: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Export status */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							export: components["schemas"]["BiQueryExport"];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["BiForbidden"];
				404: components["responses"]["BiNotFound"];
				500: components["responses"]["BiServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/cli-auth/start": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Begin a CLI device-authorization flow
		 * @description RFC 8628 device-authorization-grant — first leg. Mints a
		 *     `(device_code, user_code)` pair: the CLI keeps the device_code secret
		 *     and polls `/cli-auth/poll`, the human-readable user_code is shown to
		 *     the user who pastes it into the browser at `verification_uri`. The
		 *     device_code is hashed (SHA-256) before storage so a DB leak cannot
		 *     impersonate an in-flight CLI session. The user_code uses a confusion-
		 *     free 20-letter alphabet (no 0/O/1/I/L) and is formatted `XXXX-YYYY`
		 *     for easy reading aloud. `interval: 2` is the recommended poll cadence
		 *     in seconds. The flow expires after 10 minutes — re-call `/start` to
		 *     restart. This is the endpoint `jc auth login --browser` (master plan
		 *     Phase 0.5) hits first.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: {
				content: {
					"application/json": {
						/** @description Display string shown on the browser confirm page (e.g. `raj-laptop`). Sanitized to strip control characters and pipes. Defaults to `unknown CLI` and is capped at 120 chars. */
						hostname?: string;
					};
				};
			};
			responses: {
				/** @description Pending device code created. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @description 64-char hex secret the CLI keeps and polls with. Never shown to humans. */
							device_code?: string;
							/** @description Human-typeable code in XXXX-YYYY format. Shown to the user; pasted in browser. */
							user_code?: string;
							/** @description Dashboard URL the user opens to approve. */
							verification_uri?: string;
							/** @description Same URL pre-filled with the user_code + hostname querystring. */
							verification_uri_complete?: string;
							/** @description TTL in seconds (currently 600). */
							expires_in?: number;
							/** @description Recommended poll interval in seconds. */
							interval?: number;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/cli-auth/info": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Look up a user_code's metadata for the authorize page
		 * @description Returns the hostname submitted on `/cli-auth/start` plus the code's
		 *     expiry timestamp so the browser-side authorize page can render a
		 *     friendly "Authorize raj-laptop?" prompt and a countdown. Unauthenticated
		 *     — the user_code itself is the bearer that gates this lookup. The
		 *     endpoint returns 404 for unknown codes, 410 for codes that have already
		 *     been used or have expired; treat 410 as "ask the user to re-run
		 *     `jc auth login`." Errors are structured `JustcrawlError` bodies (Phase
		 *     0.6 contract) so the dashboard's authorize page can render code-aware
		 *     copy without parsing.
		 */
		get: {
			parameters: {
				query: {
					/** @description The XXXX-YYYY code the user pasted into the browser. */
					user_code: string;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Code metadata. Used by the dashboard authorize page to render the confirm dialog. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @description Sanitized hostname the CLI submitted on /start. */
							hostname?: string;
							/** Format: date-time */
							expires_at?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				/** @description Unknown `user_code`. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Code expired or already used. */
				410: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/cli-auth/approve": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Approve a pending CLI device code from the browser
		 * @description Browser-side leg of the device-authorization flow. Mints a new API key
		 *     bound to the calling user's org (named `jc CLI on <hostname>`) and
		 *     attaches the plaintext key to the cli_auth_codes row so the CLI's next
		 *     `/cli-auth/poll` receives it. Lateral-movement defense: this endpoint
		 *     refuses Bearer-token (`sr_live_*`) auth — only a browser JWT can
		 *     approve a CLI session. That blocks an attacker holding a leaked API
		 *     key from minting a second persistent key with a benign-looking name.
		 *     Race-safe: if a concurrent expiry/approval wins between the pre-check
		 *     and the UPDATE, the minted key is kept (caller can delete it from the
		 *     dashboard) and a 410 is returned so the UI can retry.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						user_code: string;
					};
				};
			};
			responses: {
				/** @description Approved. Body echoes hostname so the dashboard can render confirmation. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @example true */
							ok?: boolean;
							hostname?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				/** @description Browser JWT required — API-key Bearer auth is refused here (lateral-movement defense). */
				403: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Unknown `user_code`. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Code expired or already used (includes the post-race lost-update case). */
				410: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/cli-auth/poll": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Poll for the result of a CLI device-authorization flow
		 * @description Final leg of the device-authorization flow. The CLI polls this endpoint
		 *     at the `interval` returned by `/cli-auth/start` (currently every 2s);
		 *     once the user approves in the browser, this endpoint returns the API
		 *     key plaintext exactly ONCE. A second poll after success returns 410
		 *     `auth_code_consumed` — the CLI must persist the key on the first
		 *     success response. Body branches:
		 *     `{ status: "pending" }` while the user hasn't approved yet;
		 *     `{ status: "approved", api_key, org_id, org_name, user_email }` on the
		 *     single success read. Subject to `cliAuthLimiter`. This is the endpoint
		 *     `jc auth login --browser` (master plan Phase 0.5) loops on after
		 *     opening the verification URL.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						device_code: string;
					};
				};
			};
			responses: {
				/** @description Poll result. Either still-pending or approved-with-key. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {string} */
							status?: "pending";
						} | {
							/** @enum {string} */
							status?: "approved";
							/** @description Plaintext API key. Visible exactly ONCE. Persist immediately. */
							api_key?: string;
							/** Format: uuid */
							org_id?: string;
							org_name?: string | null;
							/** Format: email */
							user_email?: string | null;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				/** @description Unknown `device_code`. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Code expired or already consumed (post-approval second poll). */
				410: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/results": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List extraction results
		 * @description Returns the most recent landed extraction results for the caller's org, newest first.
		 *     Org-scoped — a caller never sees another tenant's rows.
		 *
		 *     Response includes `indexingPending`: the count of this org's jobs that have completed
		 *     extraction but are still waiting for the bi-postgres bulk-writer to flush (Phase 1 Lane D
		 *     interim state). The dashboard surfaces an "N extractions indexing" banner above the list
		 *     when this is > 0 so the user understands why a fresh scrape isn't on the list yet.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Filter by domain (e.g. `walmart.com`). Optional. */
					domain?: string;
					/** @description Page size cap. Values above 200 are clamped to 200. */
					limit?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description List of extraction results plus the in-flight indexing counter. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["ExtractionResult"][];
							/** @description Count of this org's jobs sitting in `extraction_done` awaiting the bulk-writer flush. When > 0, the UI surfaces an indexing banner above the list. */
							indexingPending?: number;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/results/{jobId}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get extraction result for a job
		 * @description Fetches the extraction result for one job, with explicit state codes for the
		 *     extraction-done → bulk-writer-flush handoff (Phase 1 Lane D). Three terminal shapes:
		 *
		 *     - **200** — bulk-writer landed the row; full `ExtractionResult` returned.
		 *     - **202** — job is in `extraction_done`, awaiting the next bi-postgres flush. The
		 *       response sets both the standard `Retry-After` HTTP header and a body field
		 *       `retryAfterSeconds` so polling clients can drive their backoff from either source.
		 *       Body shape `{ indexingPending: true, ... }` is the discriminator the React polling
		 *       hook union-narrows on.
		 *     - **500** — job is `completed` but no `extraction_results` row landed (bulk-writer
		 *       ack mismatch). The backend's `stuck_extraction_done_rows` system_job cron alerts
		 *       ops independently; this surface returns 500 so the UI shows "something went wrong"
		 *       instead of spinning forever.
		 *
		 *     404 covers every other job status (`pending` / `running` / `extracting` / `failed`)
		 *     and missing jobs alike — by design, an org never learns whether a missing UUID was
		 *     another tenant's job or never existed.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					jobId: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Extraction result row has landed. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["ExtractionResult"];
					};
				};
				/** @description Extraction completed; awaiting bulk-writer flush. Retry after `retryAfterSeconds` (also sent as the standard `Retry-After` HTTP header). */
				202: {
					headers: {
						/** @description Suggested delay in seconds before the next poll. */
						"Retry-After"?: number;
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {boolean} */
							indexingPending?: true;
							/** @enum {string} */
							status?: "extraction_done";
							retryAfterSeconds?: number;
							message?: string;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/schemas": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List extraction schemas
		 * @description Read-only list of XPath schemas the platform has discovered for cross-org sites.
		 *     Schemas are global by design — they describe page structure, not tenant data, so the
		 *     same `(domain, pageType)` pair is shared across all orgs that scrape it. Org-specific
		 *     overrides live on the `/attributes/{domain}` endpoint and are merged at extraction time.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Optional. Filter to one site (e.g. `walmart.com`). */
					domain?: string;
					/** @description Optional. Filter to one page-type taxonomy. */
					pageType?: "product" | "product_list" | "serp" | "article" | "job_posting" | "bestseller";
					/** @description Page size cap. Values above 200 are clamped to 200. */
					limit?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description List of extraction schemas. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["ExtractionSchema"][];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/schemas/{domain}/{pageType}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get latest schema for a domain + page type
		 * @description Returns the highest-version platform-discovered schema for the `(domain, pageType)`
		 *     pair, or 404 if the platform has not yet discovered one. New schemas land asynchronously
		 *     when the extraction worker runs an LLM-driven XPath discovery on a fresh page — there's
		 *     no synchronous "create schema" endpoint, by design.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					domain: string;
					pageType: "product" | "product_list" | "serp" | "article" | "job_posting" | "bestseller";
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Latest extraction schema for the requested pair. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["ExtractionSchema"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/discover": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Trigger extraction schema discovery for an already-scraped job
		 * @description Re-drives extraction against a job's **cached** HTML — the provider is never
		 *     re-hit. When no extraction schema covers the requested attributes, this also
		 *     triggers LLM XPath discovery for the job's domain and page type.
		 *
		 *     Discovery is shared across orgs and costs LLM budget, so it runs at most once
		 *     per domain/page-type: a schema that already covers every requested attribute
		 *     is reused as-is, and a schema missing some of them is topped up rather than
		 *     rediscovered from scratch.
		 *
		 *     Returns `202` — the work happens asynchronously. Poll
		 *     `GET /api/v1/extraction/schemas/{domain}/{pageType}` for the schema and
		 *     `GET /api/v1/extraction/results/{jobId}` for this job's extracted values.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/**
						 * Format: uuid
						 * @description A completed job owned by the caller's org, whose body blob is still retained.
						 */
						jobId: string;
						/**
						 * @default product
						 * @enum {string}
						 */
						pageType?: "product" | "product_list" | "serp" | "article" | "job_posting";
						/**
						 * @description Attribute names to ensure the schema covers. Omit for the page
						 *     type's full default attribute set. Names must match
						 *     `^[a-z][a-z0-9_]*$`.
						 */
						attributes?: string[];
					};
				};
			};
			responses: {
				/** @description Discovery request accepted and queued. */
				202: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @description The canonical (www-stripped) domain the schema is keyed on. */
							domain?: string;
							pageType?: string;
						};
					};
				};
				/** @description Missing `jobId`, a `pageType` outside the enum, or an `attributes` list that is too long or contains a malformed name. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description No such job in the caller's org. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description The job's cached body is gone (never stored, or past its retention window), so there is nothing to re-extract. */
				410: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/attributes/{domain}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List org custom attributes for a domain
		 * @description Reads this org's per-domain custom attribute overrides for the page type. These layer
		 *     on top of the platform-discovered schema at extraction time — the merged set is what
		 *     the extractor runs. Returns `{ attributes: [] }` (not 404) when the org has not yet
		 *     customized the domain, so the dashboard can render the editor without a second probe.
		 */
		get: {
			parameters: {
				query?: {
					pageType?: string;
				};
				header?: never;
				path: {
					domain: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Org-defined custom attributes for the domain + page type (empty array when none defined). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["OrgAttributes"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		/**
		 * Create or update org custom attributes
		 * @description Upserts this org's custom attribute set for the `(domain, pageType)` pair, replacing
		 *     any prior set. The supplied attributes are merged with the platform schema at extraction
		 *     time — same-name attributes override the platform's; new names extend the result shape.
		 *     Requires `workflows:write` (admin / editor roles only).
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					domain: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						pageType: string;
						attributes: components["schemas"]["AttributeSchema"][];
					};
				};
			};
			responses: {
				/** @description Updated org attributes. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["OrgAttributes"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		/**
		 * Remove org custom attributes
		 * @description Drops this org's custom attribute set for the `(domain, pageType)` pair. Subsequent
		 *     extractions revert to the platform-discovered schema for that page type. Idempotent —
		 *     calling on a domain without overrides still returns `{ ok: true }`.
		 *     Requires `workflows:write`.
		 */
		delete: {
			parameters: {
				query?: {
					pageType?: string;
				};
				header?: never;
				path: {
					domain: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Custom attribute set removed (or already absent). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							ok?: boolean;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/test-xpath": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Test XPath attributes against stored HTML
		 * @description Synchronously re-runs extraction with the supplied attribute set against an existing
		 *     cached HTML blob — the dashboard's "Try this XPath before saving" surface. The blob is
		 *     sourced from either the explicit `jobId` (when supplied) or the most recent job with a
		 *     stored body for the domain. No new scrape is dispatched.
		 *
		 *     Capped at 5MB of HTML per call; larger blobs return 413 rather than time out. The
		 *     extractor is run with a one-shot schema (`id: 'test'`, version: 1) so this never
		 *     mutates the org's saved attributes — use `PUT /attributes/{domain}` for that.
		 *     Requires `workflows:write`.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						domain: string;
						pageType: string;
						attributes: components["schemas"]["AttributeSchema"][];
						/**
						 * Format: uuid
						 * @description Optional. If omitted, uses the most recent job with stored HTML for the domain.
						 */
						jobId?: string;
					};
				};
			};
			responses: {
				/** @description Extracted values, quality scores, and the source job's URL. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							values?: {
								[key: string]: unknown;
							};
							qualityScore?: {
								completeness?: number;
								validation?: number;
								composite?: number;
							};
							jobId?: string;
							url?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				/** @description HTML blob exceeds the 5MB sync limit. Narrow the test set or split the page. */
				413: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/backfill": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Re-extract historical results with the current schema
		 * @description Replays the current merged schema (platform-discovered + this org's custom attributes)
		 *     against historical raw HTML blobs and updates the `extraction_results` rows in place.
		 *     Useful after editing custom attributes — old rows pick up the new fields without a
		 *     re-scrape.
		 *
		 *     **Sync path, hard capped at 20 rows per call.** Either pass `resultIds` to target an
		 *     exact set, or omit it to process the most recent N (defaults to 20, max 50) results
		 *     for the `(domain, pageType)` pair. Each row is processed sequentially; per-row
		 *     failures show up as `skipped` / `failed` in the summary rather than aborting the run.
		 *     Requires `workflows:write`.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						domain: string;
						pageType: string;
						resultIds?: string[];
						/** @default 20 */
						limit?: number;
					};
				};
			};
			responses: {
				/** @description Per-row backfill summary with status (`updated` / `skipped_no_blob` / `skipped_too_large` / `error`). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							processed?: number;
							skipped?: number;
							failed?: number;
							results?: {
								id?: string;
								status?: string;
								newFields?: number;
							}[];
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/urls/{urlItemId}/extractions": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List extraction history for one URL item
		 * @description Returns paginated extraction history for a single URL item — every prior extraction the
		 *     org has on that URL, newest first, plus the canonical `url` and `domain` strings so the
		 *     detail page can render headers without a second probe. Org-scoped; a 404 on a URL the
		 *     caller doesn't own is indistinguishable from a missing URL by design.
		 *
		 *     The `indexingPending` field counts this URL's jobs that sit in `extraction_done`
		 *     awaiting the bulk-writer flush (Phase 1 Lane D); the UI uses it to show "N indexing"
		 *     above the table.
		 */
		get: {
			parameters: {
				query?: {
					page?: number;
					pageSize?: number;
				};
				header?: never;
				path: {
					urlItemId: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated extraction history for the URL item. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["ExtractionResult"][];
							total?: number;
							url?: string | null;
							domain?: string | null;
							/** @description Count of this URL's jobs sitting in `extraction_done` awaiting the bulk-writer flush (Phase 1 Lane D). */
							indexingPending?: number;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/results/by-id/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get one extraction result by its own ID
		 * @description Fetches a single extraction row by its extraction ID. Use this when you already
		 *     hold an extraction ID — from `GET /api/v1/extraction/results` or a warehouse
		 *     delivery. To look one up by the job that produced it, use
		 *     `GET /api/v1/extraction/results/{jobId}` instead; the `/by-id/` segment exists
		 *     precisely so the two lookups do not collide.
		 *
		 *     The response includes the scraped `url` alongside the extraction so rendering a
		 *     result does not need a second request.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Extraction result ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description The extraction result, plus the URL it came from. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							id?: string;
							/** Format: uuid */
							jobId?: string;
							domain?: string;
							dataType?: string;
							schemaId?: string | null;
							normalized?: {
								[key: string]: unknown;
							};
							qualityCompleteness?: number | null;
							qualityValidation?: number | null;
							qualityComposite?: number | null;
							latencyMs?: number | null;
							/** Format: date-time */
							deliveredAt?: string | null;
							/** Format: date-time */
							createdAt?: string;
							/**
							 * Format: uri
							 * @description Scraped URL; null if the source URL row was deleted.
							 */
							url?: string | null;
							/** @description Whether raw HTML is retained, without exposing its key. */
							hasRawBlob?: boolean;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Extraction not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/extraction/results/{id}/raw": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get access to an extraction's raw HTML
		 * @description Returns a way to reach the raw HTML the extraction was derived from — useful for
		 *     debugging a selector that stopped matching, or for re-parsing without re-scraping.
		 *
		 *     The response shape depends on where the blob lives, so branch on `kind`:
		 *
		 *     - `presigned` — the blob is in JustCrawl's bucket; `url` is a signed S3 link
		 *       valid for one hour. Fetch it directly, and re-request this endpoint rather
		 *       than caching the link.
		 *     - `key` — the blob is in the org's own bucket; `blobKey` is the object key to
		 *       read with your own credentials.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Extraction result ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Either a signed URL or an object key, depending on which bucket holds the blob. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {string} */
							kind: "presigned";
							/**
							 * Format: uri
							 * @description Signed S3 URL, valid for one hour.
							 */
							url: string;
						} | {
							/** @enum {string} */
							kind: "key";
							/** @description Object key in the org's own bucket. */
							blobKey: string;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Extraction not found, or it has no stored raw blob. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/inputs": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List input queue configs
		 * @description Returns every input queue config configured for the org — SQS pollers
		 *     and webhook receivers that push URLs into the scrape pipeline from
		 *     upstream systems. Each row carries a derived `webhookUrl` field
		 *     computed at response time (`/api/v1/webhooks/{webhookToken}/ingest`)
		 *     so the dashboard can render the endpoint to copy without round-
		 *     tripping through a separate token-resolve call. Encrypted credentials
		 *     are not returned; only metadata + the queue URL + the webhook token
		 *     (when present). Requires the `org:manage` permission.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Array of input queue configurations (webhook URL pre-computed per row). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["InputQueueConfig"][];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		/**
		 * Create input queue config
		 * @description Provisions a new upstream input source. Two `queueType` paths:
		 *     `sqs` pulls URLs from an external SQS queue on the customer's AWS
		 *     account (requires `credentials.accessKeyId` + `secretAccessKey` +
		 *     `region` and a `queueUrl`); `webhook` mints a signed ingest URL the
		 *     customer's upstream can POST URLs to. Credentials are AES-256-GCM
		 *     encrypted at rest before persistence. `pollIntervalMs` is bounded
		 *     (1s..1h) and `maxUrlsPerDay` is bounded (1..1M) — out-of-range values
		 *     return 400 with a specific message naming the limit. The response
		 *     carries the derived `webhookUrl` ready to paste into the upstream
		 *     configuration. Requires the `org:manage` permission.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description Display name shown in the dashboard. */
						name: string;
						/** @enum {string} */
						queueType: "sqs" | "webhook";
						/** @description For `sqs`, requires `accessKeyId`, `secretAccessKey`, `region`. Encrypted at rest before persistence. Omit for `webhook`. */
						credentials?: Record<string, never>;
						/** @description Required when `queueType=sqs` — the SQS queue ARN/URL the poller will read. */
						queueUrl?: string;
						/** @description Optional. Workflow to route ingested URLs through; falls back to Automatic resolution when omitted. */
						workflowId?: string;
						/** @description SQS poll cadence in ms. Bounded 1s..1h. */
						pollIntervalMs?: number;
						/** @description Daily ingestion cap (defense against runaway upstreams). */
						maxUrlsPerDay?: number;
					};
				};
			};
			responses: {
				/** @description Created. Body includes the derived `webhookUrl` for `queueType=webhook`, or null otherwise. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["InputQueueConfig"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/inputs/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		/**
		 * Update input config
		 * @description Partial update — every body field is optional. Omitted fields preserve
		 *     their existing value. Re-submitted `credentials` are re-encrypted before
		 *     persistence (the encrypted blob is regenerated on every update, not
		 *     diffed). The same bounded-range validation applies as on create
		 *     (`pollIntervalMs` 1s..1h, `maxUrlsPerDay` 1..1M). Requires the
		 *     `org:manage` permission.
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Input queue config ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: {
				content: {
					"application/json": {
						name?: string;
						credentials?: Record<string, never>;
						queueUrl?: string;
						workflowId?: string;
						pollIntervalMs?: number;
						maxUrlsPerDay?: number;
					};
				};
			};
			responses: {
				/** @description Updated input queue configuration. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["InputQueueConfig"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Input queue config not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		/**
		 * Delete input config
		 * @description Removes the input queue config. For `queueType=sqs` the poller stops on
		 *     the next poll cycle (cache TTL up to `pollIntervalMs`); for
		 *     `queueType=webhook` the signed ingest URL begins returning 404
		 *     immediately. Already-ingested URLs are preserved. Requires the
		 *     `org:manage` permission.
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Input queue config ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Input config deleted. */
				204: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Input queue config not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/inputs/{id}/toggle": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		/**
		 * Toggle input config
		 * @description Pauses or resumes the input source without deleting it. A paused SQS
		 *     config stays in the dashboard, keeps its credentials encrypted at rest,
		 *     and resumes from the next poll cycle when re-enabled. A paused webhook
		 *     config begins returning 404 on its signed ingest URL until re-enabled.
		 *     Use this for incident-mode pauses; use DELETE to remove permanently.
		 *     Requires the `org:manage` permission.
		 */
		patch: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						isEnabled: boolean;
					};
				};
			};
			responses: {
				/** @description Toggled. Body echoes `{ id, isEnabled }`. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							id?: string;
							isEnabled?: boolean;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Input queue config not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		trace?: never;
	};
	"/api/v1/integrations/inputs/{id}/test": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Test input connection (coming soon)
		 * @description Placeholder for the round-trip connection test on an input queue config
		 *     (S3 bucket reachability, SQS receive-message dry run, webhook secret
		 *     echo). Currently returns `501 Not Implemented` — surfaced in the spec
		 *     so customers integrating against the dashboard surface get a stable
		 *     501 instead of a 404 while the live test ships. Requires the
		 *     `org:manage` permission.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Input queue config ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Endpoint not yet implemented. Stable 501 contract — wait for the implementation announcement before wiring callers. */
				501: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/outputs": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List output configs
		 * @description Returns every output destination configured for the org — S3 buckets
		 *     and HMAC-signed webhook destinations that downstream workflow nodes
		 *     can write to. The per-org `internal` output_config (system-managed,
		 *     provisioned by BI v3 Phase 0) is **excluded** from this list — it's
		 *     not user-editable and surfacing it would let users try to delete or
		 *     edit it. Fetch the internal config via
		 *     `GET /api/v1/integrations/outputs/internal` instead. Encrypted
		 *     credentials are not returned; only metadata + destination. Requires
		 *     the `org:manage` permission.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Array of customer-managed output configurations (excludes the system-managed `internal` config). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["OutputConfig"][];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		/**
		 * Create output config
		 * @description Provisions a new downstream output destination. Two `outputType` paths:
		 *     `s3` writes job results to a customer-owned bucket (requires
		 *     `credentials.accessKeyId` + `secretAccessKey` + `region` and a
		 *     `destination` bucket name); `webhook` POSTs results to a customer URL
		 *     (requires `credentials.hmacSecret` for signature verification, and the
		 *     `destination` must be a public URL — SSRF defense blocks localhost /
		 *     private IPs / `.internal` / `.local` hostnames / the AWS metadata IP).
		 *     Credentials are AES-256-GCM encrypted at rest. The system-managed
		 *     `internal` outputType is provisioned automatically per-org by
		 *     migration V096 and cannot be created via this endpoint. Requires the
		 *     `org:manage` permission.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						name: string;
						/** @enum {string} */
						outputType: "s3" | "webhook";
						/** @description For `s3`, requires `accessKeyId` + `secretAccessKey` + `region`. For `webhook`, requires `hmacSecret`. Encrypted at rest. */
						credentials: Record<string, never>;
						/** @description For `s3`, the bucket name. For `webhook`, the public POST URL. SSRF-protected on submit. */
						destination: string;
					};
				};
			};
			responses: {
				/** @description Created output configuration. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["OutputConfig"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/outputs/internal": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get the system-managed internal output config
		 * @description Returns the per-org `internal` `output_config` row provisioned by the
		 *     BI v3 Phase 0 0e backfill (V096). This config is the FK target every
		 *     auto-migrated `bigdata` workflow node references; the workflow editor
		 *     reads it to know which `outputConfigId` to set when inserting a new
		 *     bigdata node ("Add Internal warehouse" remediation, 0h). Steady-state
		 *     every org has one — 404 only fires on legacy orgs that predate the
		 *     V096 backfill, which should not exist in production.
		 *
		 *     Hidden from `GET /api/v1/integrations/outputs` by design — surfacing
		 *     it on the Integrations page would let `org:manage` users try to
		 *     delete or edit it. `DELETE /api/v1/integrations/outputs/{id}` also
		 *     refuses the internal config with 409.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description The org's internal output configuration. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["OutputConfig"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/outputs/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		/**
		 * Update output config
		 * @description Partially updates an existing output destination. Supply any subset of
		 *     `name` / `credentials` / `destination`; omitted fields stay as-is.
		 *     When `credentials` is supplied, the new value is AES-256-GCM
		 *     re-encrypted at rest before write — the old value is overwritten in
		 *     place. When `destination` is supplied for a webhook output, the same
		 *     SSRF defense as `POST /outputs` rejects localhost / private IPs /
		 *     `.internal` / `.local` / AWS-metadata-IP destinations.
		 *
		 *     The system-managed `internal` config cannot be edited via this
		 *     endpoint — pass its id and the storage layer will silently reject
		 *     (system-managed flag is enforced at the DB layer, not this route).
		 *     Requires the `org:manage` permission.
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Output config ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						name?: string;
						/** @description New credentials object. Same shape as `POST /outputs` (S3 requires accessKeyId+secretAccessKey+region; webhook requires hmacSecret). Re-encrypted on save. */
						credentials?: Record<string, never>;
						/** @description New destination. Webhook URLs are SSRF-checked on update. */
						destination?: string;
					};
				};
			};
			responses: {
				/** @description Updated output configuration. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["OutputConfig"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		/**
		 * Delete output config
		 * @description Removes a customer-managed output destination. Refuses to delete the
		 *     system-managed `internal` outputType (409 Conflict) — every workflow
		 *     with an auto-migrated `bigdata` node references it via FK, and
		 *     deleting it would brick the BI invariant. Past deliveries written to
		 *     this destination are preserved; only future writes will fail. To
		 *     temporarily disable instead, use
		 *     `PATCH /api/v1/integrations/outputs/{id}/toggle` with
		 *     `isEnabled: false`. Requires the `org:manage` permission.
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Output config ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Output config deleted. */
				204: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Output config not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				409: components["responses"]["Conflict"];
				500: components["responses"]["ServerError"];
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/outputs/{id}/toggle": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		/**
		 * Enable or disable an output config
		 * @description Soft pause / resume on an output destination — flips `isEnabled`
		 *     without deleting the config or wiping its credentials. Workflow nodes
		 *     referencing a disabled output skip the delivery without erroring out
		 *     the run; useful for incident-mode pause when a downstream is misbehaving
		 *     (rotating webhook secret, S3 bucket maintenance, billing investigation)
		 *     without forcing a full re-provision afterwards.
		 *
		 *     The `internal` system-managed config can be toggled — the BI Phase 0
		 *     invariant is on identity / FK presence, not on enabled state — but
		 *     doing so will silently drop bigdata-node writes. Use with care.
		 *     Requires the `org:manage` permission.
		 */
		patch: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Output config ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						isEnabled: boolean;
					};
				};
			};
			responses: {
				/** @description New isEnabled state. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							id?: string;
							isEnabled?: boolean;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		trace?: never;
	};
	"/api/v1/integrations/outputs/{id}/test": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Test output connection (coming soon)
		 * @description Placeholder for the round-trip connection test on an output config
		 *     (S3 PutObject + DeleteObject dry run for s3 outputs, signed webhook
		 *     POST roundtrip for webhook outputs). Currently returns `501 Not
		 *     Implemented` — surfaced in the spec so customers integrating against
		 *     the dashboard surface get a stable 501 instead of a 404 while the
		 *     live test ships. Requires the `org:manage` permission.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Output config ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Endpoint not yet implemented. Stable 501 contract — wait for the implementation announcement before wiring callers. */
				501: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/integrations/storage": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get the org's blob-storage config
		 * @description Returns where job result blobs (HTML / JSON) are written for this org —
		 *     either the JustCrawl-managed platform bucket (`type: "platform"`,
		 *     default for every org) or a customer-owned S3 bucket
		 *     (`type: "custom"`, set via `PUT /storage`). When custom, the response
		 *     also includes the bucket name and whether S3 versioning is enabled on
		 *     that bucket (drives the `_v1`/`_v2`/... suffix fallback when off).
		 *
		 *     `documentExpiryDays` is the org's retention setting — null means
		 *     indefinite (platform storage caps this internally). On custom buckets
		 *     this drives the `justcrawl-retention` S3 Lifecycle rule installed by
		 *     `PUT /storage`. Credentials are never returned; only metadata.
		 *     Requires the `org:manage` permission.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Current storage config. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {string} */
							type?: "platform" | "custom";
							bucket?: string | null;
							storageVersioningEnabled?: boolean | null;
							documentExpiryDays?: number | null;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		/**
		 * Switch storage between platform and customer S3 bucket
		 * @description Switches where this org's job result blobs are written. Two paths:
		 *
		 *     - `type: "platform"` — reset to JustCrawl-managed storage. Bucket and
		 *       credentials fields are ignored. Side-effect free; the existing
		 *       encrypted bucket credentials (if any) are wiped from the DB.
		 *     - `type: "custom"` — switch to a customer-owned S3 bucket. Requires
		 *       `bucket` + `accessKeyId` + `secretAccessKey` + `region`. Credentials
		 *       are validated synchronously by uploading then deleting a test object
		 *       at `.justcrawl-test-<timestamp>` against the supplied bucket — if
		 *       that fails, the call returns 400 with the AWS error and does not
		 *       persist anything. On success the bucket's `GetBucketVersioning` is
		 *       read to set `storageVersioningEnabled` (drives the `_v1`/`_v2`
		 *       suffix fallback); credentials are AES-256-GCM encrypted at rest.
		 *
		 *     Optional `documentExpiryDays` (1–3650, or null for indefinite) drives
		 *     the `justcrawl-retention` S3 Lifecycle rule installed best-effort
		 *     (existing customer lifecycle rules with a different ID are preserved).
		 *     If the lifecycle PUT fails (IAM permission gap), the response includes
		 *     a `lifecycleWarning` field — the storage swap still succeeds. If the
		 *     bucket has versioning disabled, the response includes a
		 *     `versioningWarning` field.
		 *
		 *     Requires the `org:manage` permission.
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @enum {string} */
						type: "platform" | "custom";
						/** @description Required when `type=custom`. S3 bucket name. */
						bucket?: string;
						/** @description Required when `type=custom`. Validated by a PutObject + DeleteObject roundtrip. */
						accessKeyId?: string;
						/** @description Required when `type=custom`. AES-256-GCM encrypted at rest. */
						secretAccessKey?: string;
						/** @description Required when `type=custom`. AWS region (e.g. `us-east-1`). */
						region?: string;
						/** @description 1–3650 inclusive, or null for indefinite. Drives the bucket's S3 Lifecycle rule (best-effort). */
						documentExpiryDays?: number | null;
					};
				};
			};
			responses: {
				/** @description New storage config. Includes optional `versioningWarning` and `lifecycleWarning` fields when applicable. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {string} */
							type?: "platform" | "custom";
							bucket?: string | null;
							storageVersioningEnabled?: boolean;
							versioningWarning?: string;
							lifecycleWarning?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/jobs": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List jobs
		 * @description Paginated list of scrape jobs in the current org. Filters compose: pass any combination of
		 *     `status`, `workflowId`, `scheduleId`, `domain`, `tag`, plus the timestamp range filters
		 *     (`createdAfter`/`createdBefore`/`updatedAfter`/`updatedBefore`). Heavy fields
		 *     (`executionTrace`, `nodeState`) are stripped from list responses — fetch the single-job
		 *     endpoint to get them. Each item carries `cost` derived from the org's vendor-cost table.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Page number (default 1) */
					page?: number;
					/** @description Items per page (default 25, max 100) */
					pageSize?: number;
					/** @description Filter by job status */
					status?: "pending" | "running" | "completed" | "failed" | "waiting_retry" | "extracting" | "extraction_done";
					/** @description Filter by workflow ID */
					workflowId?: string;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated list of jobs */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["Job"][];
							total?: number;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		/**
		 * Submit a scrape job
		 * @description Queue a job for an existing URL item against a workflow. If `workflowId` is omitted the
		 *     gateway resolves the workflow via the URL's domain route. Returns `201` immediately
		 *     with the new `jobId` and `status: pending`; poll `GET /api/v1/jobs/{jobId}` (or use the
		 *     batch endpoint) for completion. Quota gate applies: `402 Insufficient credits` when the
		 *     org's quota is exhausted, with `remainingCredits` echoed back in the body.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/**
						 * Format: uuid
						 * @description ID of the URL item to scrape. Create one via `POST /api/v1/urls`.
						 */
						urlItemId: string;
						/**
						 * Format: uuid
						 * @description Optional. When omitted, resolves from the URL's domain route.
						 */
						workflowId?: string;
					};
				};
			};
			responses: {
				/** @description Job created successfully */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							jobId: string;
							/**
							 * @description Always `pending` on creation. Poll `GET /api/v1/jobs/{jobId}` for the real-time status.
							 * @enum {string}
							 */
							status: "pending";
							url: string;
							/** @description Present only when `workflowId` was omitted and the gateway resolved it via the URL's domain route. */
							resolvedWorkflow?: Record<string, never>;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				/** @description Insufficient credits */
				402: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						/**
						 * @example {
						 *       "error": "QUOTA_EXCEEDED",
						 *       "message": "Credits exhausted. Recharge to continue.",
						 *       "plan": "free",
						 *       "remainingCredits": 0,
						 *       "isTrialExpired": false
						 *     }
						 */
						"application/json": {
							/** @enum {string} */
							error: "QUOTA_EXCEEDED";
							message: string;
							plan?: string | null;
							remainingCredits?: number | null;
							isTrialExpired?: boolean | null;
						};
					};
				};
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/jobs/batch-status": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Batch poll job statuses */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						jobIds: string[];
					};
				};
			};
			responses: {
				/**
				 * @description Array of compact job-status records — one per requested `jobIds` entry that
				 *     exists in this org (missing IDs are silently omitted, never error). Returned
				 *     as a bare array, not wrapped under a key.
				 */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							id: string;
							/** @description Current job status (e.g. pending, processing, completed, failed). */
							status: string;
							/** @description Workflow node currently executing, or null when terminal. */
							currentNodeId?: string | null;
							/** Format: date-time */
							updatedAt: string;
						}[];
					};
				};
				/** @description Missing/empty `jobIds` array, or more than 100 IDs per request. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/jobs/stats": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Dashboard-widget aggregated counters with prior-window deltas */
		get: {
			parameters: {
				query?: {
					/** @description Failure-counter window in seconds (default 3600, clamped [60, 86400]) */
					windowSec?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Job counters for the org */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/jobs/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Get job detail */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Job ID */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Job detail with execution trace */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Job"];
					};
				};
				/** @description Job not found */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/jobs/{id}/result": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get scrape result metadata + presigned blob URL
		 * @description Returns a JSON envelope describing the scrape result. The actual HTML body is NOT
		 *     inlined — fetch it from `resultUrl` (a presigned S3 URL, typically 15-minute TTL)
		 *     when present, or resolve `blobKey` through your own storage client when the org
		 *     uses custom S3. The endpoint never streams the body itself.
		 *
		 *     Returns `410 Gone` when the result existed but has been cleaned up under the
		 *     org's document-retention policy; the `expiredAt` timestamp tells you when.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Job ID */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Result metadata. Exactly one of `resultUrl` (platform storage) or `blobKey` (custom S3) is present. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/**
							 * Format: uri
							 * @description Presigned S3 URL for the HTML body (platform storage). Mutually exclusive with `blobKey`.
							 */
							resultUrl?: string;
							/** @description Raw S3 object key (custom storage). Mutually exclusive with `resultUrl`. */
							blobKey?: string;
							providerId: string;
							/** @description HTTP status code the provider returned for the scrape. */
							statusCode: number;
							/** @description Size of the stored body in bytes. */
							bodySize: number;
							latencyMs: number;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Job not found, or job exists but has no result blob yet (still running, or it failed before producing a body). */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Result existed but has been cleaned up under the org's document-retention policy. Body includes `expiredAt` and a human-readable `message`. */
				410: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/plans/status": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Get plan status */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Current plan status */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["PlanStatus"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/plans/transactions": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Credit transaction history */
		get: {
			parameters: {
				query?: {
					/** @description Page number (default 1) */
					page?: number;
					/** @description Items per page (default 25, max 100) */
					pageSize?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated credit transactions */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: Record<string, never>[];
							total?: number;
						};
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/plans/recharge-request/status": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Check for a pending recharge request
		 * @description Reports whether this org already has a credit recharge request awaiting review.
		 *     Poll it after `POST /api/v1/plans/recharge-request` to avoid submitting a
		 *     duplicate while the first one is still being processed.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Whether a recharge request is currently pending. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @description True while a submitted request is awaiting review. */
							hasPending?: boolean;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/plans/recharge-request": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Submit recharge request */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						orgCountry: string;
						phone: string;
						expectedMonthlyVolume: string;
						message?: string;
					};
				};
			};
			responses: {
				/** @description Recharge request created */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/providers": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List providers and their capabilities
		 * @description The catalogue of scraping providers this platform can route to, with what each one supports. This is platform-wide capability data, not your organization's connection state — it says nothing about which providers you have credentials for.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Every provider the platform supports */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/**
							 * @description Stable provider identifier used in workflow nodes
							 * @example brightdata
							 */
							id: string;
							/** @example Bright Data */
							displayName: string;
							/** @description What this provider supports. Optional flags absent from the object mean false. */
							capabilities: {
								supportsJsRendering: boolean;
								/** @description JS rendering cannot be switched off for this provider. */
								jsRenderingAlwaysOn?: boolean;
								supportsResidentialIp?: boolean;
								supportsGeoTargeting: boolean;
								/** @description ISO-3166 alpha-2 codes, or the single entry `all` when unrestricted. */
								supportedCountries: string[];
								supportsWaitForSelector: boolean;
								supportsWaitForSelectorTimeout: boolean;
								supportsScreenshot: boolean;
								supportsMarkdown: boolean;
								supportsCustomHeaders: boolean;
								supportsCustomCookies?: boolean;
								supportsParsing?: boolean;
								supportsSessions?: boolean;
								supportsDeviceEmulation?: boolean;
								supportsAsyncRequests: boolean;
								supportsAsyncWebhook?: boolean;
								supportsTargetTemplates?: boolean;
								/** @description Longest request timeout this provider accepts, in milliseconds. */
								maxTimeoutMs: number;
								/**
								 * @description Internal geo encoding; adapters convert from ISO-2 automatically.
								 * @enum {string}
								 */
								geoFormat: "iso2_lowercase" | "iso2_uppercase" | "country_name_title" | "country_name_full";
								/** @description Provider-specific quirks worth knowing before you route to it. */
								notes: string[];
							};
						}[];
					};
				};
				401: components["responses"]["Unauthorized"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/schedules": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List schedules
		 * @description Returns a paginated slice of the org's schedules, each enriched server-side
		 *     with `workflowName` (resolved from the bound workflow or `"Automatic"` when
		 *     no `workflowId` is set), `urlCount` (matching URLs at request time), and
		 *     `estCost` (domain-aware per-thousand-URL cost projection in cents). The
		 *     cost projection uses the schedule's bound workflow DAG plus the org's
		 *     per-provider pricing table — so it reflects what the next run will
		 *     actually charge under current credentials. This endpoint backs the MCP
		 *     server's `jc_schedules_list` tool.
		 */
		get: {
			parameters: {
				query?: {
					page?: number;
					pageSize?: number;
					/** @description Filter to schedules with this cadence (e.g. `hourly`, `daily`). */
					frequency?: string;
					/** @description Filter by active state. */
					isEnabled?: boolean;
					/** @description Filter to schedules whose tag filters resolve URLs on this domain. */
					domain?: string;
					/** @description Filter to schedules carrying this tag in `tagFilters`. */
					tag?: string;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated list of schedules, enriched with workflow name, URL count, and cost projection. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["Schedule"][];
							total?: number;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		/**
		 * Create schedule
		 * @description Creates a recurring schedule that fires the named workflow on the given
		 *     cadence. The `frequency` enum picks a canonical cron expression — only
		 *     `custom` requires you to supply `cronExpr` yourself. `workflowId` is
		 *     optional: when null, the scheduler resolves a workflow per URL at
		 *     trigger time via `resolveWorkflowForUrl` (domain-routed or default `*`),
		 *     which is what the dashboard surfaces as "Automatic." `tagFilters`
		 *     narrows the URL set to URLs carrying ALL listed tags. The `timezone`
		 *     string is IANA (e.g. `America/Los_Angeles`); invalid zones return 400.
		 *     Requires verified email — unverified accounts get 403.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description Display name shown in dashboard + emitted on schedule runs. */
						name: string;
						/** @description Workflow to run. Must be `status='published'`. Pass `null` for Automatic routing. */
						workflowId?: string | null;
						/** @enum {string} */
						frequency: "every_5_minutes" | "every_10_minutes" | "every_15_minutes" | "every_30_minutes" | "hourly" | "every_2_hours" | "every_3_hours" | "every_4_hours" | "every_6_hours" | "every_8_hours" | "every_12_hours" | "daily" | "weekly" | "monthly" | "custom";
						/** @description Required only when `frequency='custom'`. Standard 5-field cron syntax. */
						cronExpr?: string;
						/** @description IANA timezone (e.g. `UTC`, `America/Los_Angeles`). Defaults to `UTC`. */
						timezone?: string;
						/** @description URLs must carry ALL listed tags to be included. Max 50. */
						tagFilters?: string[];
						/** @description When false, the schedule is created but won't fire until enabled. Defaults to true. */
						isEnabled?: boolean;
					};
				};
			};
			responses: {
				/** @description Schedule created. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Schedule"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Referenced `workflowId` not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/schedules/runs": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List schedule runs
		 * @description Returns recent schedule executions across the org, newest first. Each run groups
		 *     the jobs dispatched by one firing of a schedule, so a `runId` is the handle for
		 *     correlating a batch of jobs back to the schedule that produced them.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Only return runs of this schedule. */
					scheduleId?: string;
					/** @description Look up one specific run. */
					runId?: string;
					/** @description Only return runs that targeted this domain. */
					domain?: string;
					/** @description Free-text match against the schedule name. */
					search?: string;
					/** @description Maximum runs to return. Values above 200 are clamped. */
					limit?: number;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Matching schedule runs, newest first. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": Record<string, never>[];
					};
				};
				/** @description `runId` is not a valid UUID. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/schedules/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get schedule
		 * @description Returns the schedule including its cron expression, timezone, tag filters,
		 *     next-run timestamp, and resolved workflow name (or `"Automatic"` when no
		 *     `workflowId` is set). 404 if the ID belongs to another org — the response
		 *     shape matches "not found" so cross-org enumeration is not possible.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Schedule ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Schedule detail. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Schedule"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Schedule not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		/**
		 * Update schedule
		 * @description Partial update — every field in the body is optional. Omitted fields
		 *     inherit from the existing schedule, except cron-derived ones: any of
		 *     `frequency`, `timeOfDay`, `dayOfWeek`, `dayOfMonth`, or `cronExpr`
		 *     triggers a full cron recompute against the current values, and
		 *     `nextRunAt` is rederived from the new cron + the (possibly changed)
		 *     timezone. Switching `workflowId` to an unpublished workflow returns
		 *     400 — the schedule must always reference a runnable workflow.
		 *     Requires verified email.
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						name?: string;
						workflowId?: string | null;
						/** @enum {string} */
						frequency?: "every_5_minutes" | "every_10_minutes" | "every_15_minutes" | "every_30_minutes" | "hourly" | "every_2_hours" | "every_3_hours" | "every_4_hours" | "every_6_hours" | "every_8_hours" | "every_12_hours" | "daily" | "weekly" | "monthly" | "custom";
						cronExpr?: string;
						timezone?: string;
						tagFilters?: string[];
						isEnabled?: boolean;
					};
				};
			};
			responses: {
				/** @description Updated schedule. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Schedule"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Schedule (or referenced `workflowId`) not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		/**
		 * Delete schedule
		 * @description Permanently removes the schedule. Past `schedule_runs` rows are kept
		 *     (they reference the schedule by ID and survive the delete) but no future
		 *     runs will fire. To temporarily disable instead, use
		 *     `PATCH /api/v1/schedules/{id}/toggle` with `isEnabled: false`.
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Schedule deleted. */
				204: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Schedule not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/schedules/{id}/toggle": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		/**
		 * Enable/disable schedule
		 * @description Flips the schedule's `isEnabled` flag without otherwise mutating it. A
		 *     disabled schedule stays in the dashboard, retains its cron / tags /
		 *     workflow binding, and resumes from the same cadence when re-enabled —
		 *     it just won't fire while off. Use this for incident-mode pauses; use
		 *     DELETE to remove permanently. Requires verified email.
		 */
		patch: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						isEnabled: boolean;
					};
				};
			};
			responses: {
				/** @description Schedule toggled. Body echoes `{ id, isEnabled }`. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							id?: string;
							isEnabled?: boolean;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description Schedule not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		trace?: never;
	};
	"/api/v1/schedules/{id}/trigger": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Trigger schedule now
		 * @description Fires the schedule on-demand without waiting for its next cron tick. The
		 *     gateway resolves the URL set (filtered by `tagFilters`), enqueues a job
		 *     per URL through the same dispatch path the cron tick uses, and returns
		 *     `jobsCreated` + a `scheduleRunId` you can pass to
		 *     `GET /api/v1/schedules/runs?runId=...` to follow the batch. Edge cases:
		 *     when the URL filter resolves to zero URLs the call returns 200 with
		 *     `jobsCreated: 0` and a human-readable `message` — that's an empty filter,
		 *     not an error. `SCHEDULER_MAX_URLS_PER_RUN` (default 1000) caps a single
		 *     trigger to avoid runaway batches. Subject to `requireActiveQuota`:
		 *     over-quota orgs get 402 with the standard `QUOTA_EXCEEDED` payload.
		 *     This is the endpoint the MCP server's `jc_schedules_trigger` tool wraps.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Schedule triggered. `jobsCreated` is 0 when the URL filter matched no URLs (call still succeeded). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							jobsCreated?: number;
							/** @description ID for the new `schedule_runs` row covering this trigger; omitted when no URLs matched. */
							scheduleRunId?: string;
							/** @description Present only when `jobsCreated` is 0 — explains why nothing was enqueued. */
							message?: string;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				/** @description Org over the active credit/quota allowance. */
				402: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @example QUOTA_EXCEEDED */
							error?: string;
							message?: string;
						};
					};
				};
				403: components["responses"]["Forbidden"];
				/** @description Schedule not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{workflowId}/smart": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get smart workflow config
		 * @description Returns the smart-workflow configuration row for a workflow: ranking strategy
		 *     (`success` / `cost` / `reliability` / `quality`), mode (`auto` re-ranks automatically
		 *     vs `suggest` requires the user to apply each change), and the domain the rankings are
		 *     scoped to. Org-scoped — calling on another tenant's workflow returns 404 indistinguishably
		 *     from "not a smart workflow." A regular (non-smart) workflow also returns 404.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					workflowId: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Smart workflow configuration. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["SmartConfig"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{workflowId}/smart/mode": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		/**
		 * Update auto/suggest mode
		 * @description Switches the smart workflow between `auto` (the optimization worker re-ranks providers
		 *     and updates the DAG without asking) and `suggest` (the worker writes a
		 *     `optimization_suggestions` row and the user clicks Apply). The flip takes effect on
		 *     the next optimization tick — in-flight jobs already routed under the old DAG are not
		 *     re-routed. Requires `workflows:write`.
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					workflowId: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @enum {string} */
						mode: "auto" | "suggest";
					};
				};
			};
			responses: {
				/** @description Mode updated. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {string} */
							mode?: "auto" | "suggest";
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{workflowId}/smart/suggestions": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List suggestions
		 * @description Lists every `optimization_suggestions` row for this workflow, of every status
		 *     (`pending` / `applied` / `dismissed`), newest first. The dashboard shows pending ones
		 *     as actionable cards and the rest as audit log. Org-scoped — empty array (not 404) for
		 *     workflows with zero suggestions ever written.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					workflowId: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description All suggestions for this workflow (newest first). */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Suggestion"][];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{workflowId}/smart/suggestions/{id}/apply": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Apply suggestion
		 * @description Atomically applies a pending suggestion: rewrites the workflow's provider order to
		 *     match the suggestion's `suggested_provider_order`, regenerates the DAG, and marks the
		 *     suggestion `applied`. Uses `SELECT FOR UPDATE` on the suggestion row inside a single
		 *     transaction so concurrent apply attempts from two browser tabs can't double-spend the
		 *     suggestion.
		 *
		 *     Returns 400 when the suggestion is already `applied` or `dismissed` — by design, each
		 *     suggestion is one-shot. Returns 404 for missing suggestions or non-smart workflows
		 *     (the lookup is org-scoped so cross-tenant probes 404 indistinguishably).
		 *     Requires `workflows:write`.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					workflowId: string;
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Suggestion applied; DAG regenerated. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							workflowId?: string;
							providerOrder?: string[];
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{workflowId}/smart/suggestions/{id}/dismiss": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Dismiss suggestion
		 * @description Marks a suggestion `dismissed` without applying it. The DAG stays as-is; future
		 *     optimization ticks can still write fresh suggestions if metrics keep favoring a
		 *     different ranking. Org-scoped — cross-tenant probes return 404 indistinguishably from
		 *     missing suggestions. Requires `workflows:write`.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					workflowId: string;
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Suggestion dismissed. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @enum {string} */
							status?: "dismissed";
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/suggestions": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List all pending suggestions for org
		 * @description Org-wide pending-suggestion inbox — every workflow's `pending` suggestions in one list.
		 *     Powers the dashboard's home-page nudge "N optimizations available." Already-applied or
		 *     dismissed rows are filtered out at the SQL level; use the per-workflow listing endpoint
		 *     to see history.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description All pending suggestions across the org's smart workflows. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Suggestion"][];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/urls": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List URLs
		 * @description Returns a paginated slice of the org's URL list. Use `domain`, `tag`, and
		 *     `search` to narrow; `sort` + `order` to control ordering. `pageSize` is
		 *     capped at 100 — for bulk export use repeated `page` requests rather than
		 *     a single oversized query. This is the endpoint the MCP server's
		 *     `jc_urls_list` tool wraps, so anything you can pass here an LLM agent can
		 *     also pass via Claude / Cursor / Codex.
		 */
		get: {
			parameters: {
				query?: {
					/** @description 1-indexed page number. */
					page?: number;
					/** @description Items per page; capped at 100. */
					pageSize?: number;
					/** @description Filter to URLs on this host (e.g. `amazon.com`). */
					domain?: string;
					/** @description Filter to URLs carrying this tag. */
					tag?: string;
					/** @description Free-text substring match against URL. */
					search?: string;
					/** @description Column to sort by. Invalid values return 400. */
					sort?: "url" | "domain" | "priority" | "created" | "updated" | "lastRun";
					/** @description Sort direction. */
					order?: "asc" | "desc";
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated URL list. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["UrlItem"][];
							total?: number;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		/**
		 * Create single URL
		 * @description Adds a single URL to the org's URL list. The URL is validated for syntax
		 *     and private-address blocking (RFC 1918 / loopback hosts are rejected to
		 *     prevent SSRF). Duplicate URLs within the same org return 409 — re-submitting
		 *     an existing URL is a no-op, not a way to refresh scheduling. Use
		 *     `POST /api/v1/urls/batch` for >1 URL at a time; this single-URL endpoint
		 *     exists for dashboard-driven adds and quick API smoke tests.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description Absolute URL with scheme (`http://` or `https://`). */
						url: string;
						/** @description Optional dispatch priority; higher values dispatch first when capacity is constrained. */
						priority?: number;
						/** @description Optional list of tag slugs to attach. Use the same charset rules as `POST /api/v1/urls/{id}/tags`. */
						tags?: string[];
					};
				};
			};
			responses: {
				/** @description Created URL item. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["UrlItem"];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				409: components["responses"]["Conflict"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/urls/batch": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Batch create URLs
		 * @description Adds up to 1000 URLs per request. Invalid URLs (bad scheme, private-address,
		 *     missing field) are silently filtered out and counted in the `invalid` field
		 *     of the response — the call does not fail on partial validity, so callers
		 *     can dump a noisy list and trust the gateway to keep only the good ones.
		 *     Unverified-email accounts are capped at 5 URLs total (403 with `error: 'EMAIL_NOT_VERIFIED'`
		 *     when the cap would be exceeded). Duplicates within the org are counted
		 *     in `duplicates` rather than returned as 409s — batch semantics are
		 *     best-effort upsert. For CSV uploads use `POST /api/v1/urls/upload`.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						urls: {
							url: string;
							priority?: number;
							tags?: string[];
						}[];
					};
				};
			};
			responses: {
				/** @description Batch creation result. `created` is the count of new URLs persisted; `duplicates` were skipped because the URL already exists in the org; `invalid` is present and non-zero only when some input rows failed validation. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							created?: number;
							duplicates?: number;
							invalid?: number | null;
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		/**
		 * Delete URLs in bulk
		 * @description Deletes up to a full page of URLs in one call — the counterpart to
		 *     `POST /api/v1/urls/batch`. Hard delete with the same semantics as
		 *     `DELETE /api/v1/urls/{id}`: extraction history survives, but the URLs stop
		 *     appearing in listings and are never dispatched again.
		 *
		 *     The response reports how many rows were actually removed, which can be lower
		 *     than the number of IDs sent — IDs that belong to another org, or were already
		 *     deleted, are skipped rather than erroring the whole call.
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description URL item IDs to delete. */
						ids: string[];
					};
				};
			};
			responses: {
				/** @description Count of URLs actually deleted. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** @description Rows removed; may be fewer than the IDs supplied. */
							deleted?: number;
						};
					};
				};
				/** @description `ids` missing, not an array, or empty. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/urls/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get URL detail
		 * @description Returns the full URL row including tags, priority, enabled state, last-run
		 *     timestamp, and computed display fields. 404 when the ID is unknown OR when
		 *     it belongs to another org — the response shape is identical so cross-org
		 *     enumeration cannot distinguish "does not exist" from "not yours."
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description URL item ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description URL item. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["UrlItem"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description URL not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		/**
		 * Delete URL
		 * @description Removes a URL from the org. Hard delete — existing extraction history is
		 *     preserved (extraction rows reference the URL by ID and survive the
		 *     deletion), but the URL will no longer surface in `GET /api/v1/urls` and
		 *     will not be picked up by any future schedule run. To temporarily disable
		 *     a URL without losing it, use `PATCH /api/v1/urls/{id}/toggle` with
		 *     `isEnabled: false` instead.
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description URL item ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description URL deleted. */
				204: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description URL not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		options?: never;
		head?: never;
		/**
		 * Update a URL
		 * @description Updates the target address, the scheduling priority, or both. At least one of
		 *     `url` or `priority` must be present. Only the fields you send are changed, and
		 *     only those fields come back in the response.
		 *
		 *     Changing `url` re-validates the address and is subject to the same
		 *     per-org uniqueness constraint as `POST /api/v1/urls` — reusing an address
		 *     already tracked by the org returns 409. To disable a URL rather than edit it,
		 *     use `PATCH /api/v1/urls/{id}/toggle`.
		 */
		patch: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description URL item ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/**
						 * Format: uri
						 * @description New target address. Must be a valid, scrapable URL.
						 */
						url?: string;
						/** @description Scheduling priority; higher values are dispatched first. */
						priority?: number;
					};
				};
			};
			responses: {
				/** @description The fields that were updated, echoed back with the URL's ID. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							id?: string;
							/** Format: uri */
							url?: string;
							priority?: number;
						};
					};
				};
				/** @description Neither field supplied, or one of them failed validation. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description URL not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				/** @description Another URL in this org already uses that address. */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		trace?: never;
	};
	"/api/v1/urls/{id}/toggle": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		/**
		 * Enable or disable a URL
		 * @description Flips whether a URL participates in future schedule runs. A disabled URL keeps
		 *     its ID, history, and extraction results, and stays visible in
		 *     `GET /api/v1/urls` — it is simply skipped when schedules dispatch. This is the
		 *     reversible alternative to `DELETE /api/v1/urls/{id}`.
		 */
		patch: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description URL item ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description True to resume scraping this URL, false to pause it. */
						isEnabled: boolean;
					};
				};
			};
			responses: {
				/** @description The URL's new enabled state. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							id?: string;
							isEnabled?: boolean;
						};
					};
				};
				/** @description `isEnabled` missing or not a boolean. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description URL not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		trace?: never;
	};
	"/api/v1/urls/{id}/jobs": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List scrape jobs for a URL
		 * @description Returns the scrape history for one URL, newest first. Use it to check whether a
		 *     tracked URL is succeeding over time without filtering the org-wide
		 *     `GET /api/v1/jobs` feed yourself.
		 */
		get: {
			parameters: {
				query?: {
					/** @description 1-based page number. Values above 10000 are clamped. */
					page?: number;
					/** @description Results per page. Values above 100 are clamped. */
					pageSize?: number;
					/** @description Filter to one job status. `all` is the default (no filter). */
					status?: "success" | "failed" | "all";
				};
				header?: never;
				path: {
					/** @description URL item ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated job history for this URL. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["Job"][];
							/** @description Total jobs matching the filter, across all pages. */
							total?: number;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description URL not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/urls/{id}/extractions": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List extraction history for a URL
		 * @description Returns one row per completed extraction for this URL, ordered newest
		 *     first. `fieldTypes` is derived from the latest published schema for the
		 *     URL's `(domain, dataType)` pair — it tells the UI (and SDK consumers)
		 *     which column to render as a number vs text vs link. `indexingPending`
		 *     surfaces the count of jobs sitting in the `extraction_done` interim
		 *     state waiting on the BI bulk-writer flush (Phase 1 Lane D); when
		 *     non-zero the table is showing a stale snapshot and the dashboard
		 *     overlays a "settling" badge. `pageSize` is capped at 200 here (not 100
		 *     like /urls) because extraction rows are the unit of analyst pagination.
		 */
		get: {
			parameters: {
				query?: {
					page?: number;
					pageSize?: number;
				};
				header?: never;
				path: {
					/** @description URL item ID. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Paginated extraction history with field-type hints. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							items?: components["schemas"]["ExtractionResult"][];
							total?: number;
							fieldTypes?: {
								[key: string]: string;
							};
							/** @description Count of this URL's jobs sitting in `extraction_done` awaiting the bulk-writer flush (Phase 1 Lane D). */
							indexingPending?: number;
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				/** @description URL not found. */
				404: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/webhooks/{token}/ingest": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Ingest URLs via webhook */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Webhook token for authentication */
					token: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": (string | {
						url?: string;
						workflowId?: string;
					})[];
				};
			};
			responses: {
				/** @description URLs accepted for processing */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							accepted?: number;
							jobsCreated?: number;
						};
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * List workflows
		 * @description Returns the latest version per logical workflow in the current org.
		 *     Each item is enriched with `costPerThousand` (derived from the org's
		 *     vendor-cost table) and a `smart` block when the workflow has a
		 *     smart-workflow config attached. When `?domain=` is set without an
		 *     explicit `?status=`, the gateway defaults `status=published` so the
		 *     response matches what `resolveWorkflowForUrl` actually picks at
		 *     dispatch time — drafts inherited from version bumps are not the
		 *     running workflow and would mislead UI consumers.
		 *
		 *     **Use `workflowId`, not `id`, to address a workflow.** Each item carries
		 *     both, and they are different uuids — `workflowId` is the stable logical
		 *     id every other workflow endpoint takes, while `id` is the primary key of
		 *     the returned version row and is accepted nowhere. Passing `id` to
		 *     `GET /api/v1/workflows/{id}` returns 404.
		 */
		get: {
			parameters: {
				query?: {
					/** @description Filter workflows by status. */
					status?: "draft" | "published";
					/** @description Narrow to workflows routed to this domain (route="domain:X" or default "*"); domain-specific routes sorted first. */
					domain?: string;
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description An array of workflows (latest version each), enriched with cost + smart-workflow config. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Workflow"][];
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		/**
		 * Create workflow
		 * @description Create a new workflow with a name and DAG definition. The DAG is validated
		 *     server-side via `validate()` before persistence; structural errors return 400
		 *     with a `validationErrors` array enumerating each problem. New workflows start
		 *     in `draft` status — call `POST /api/v1/workflows/{id}/publish` to make them
		 *     live. Set `autoGenerated: true` if the DAG was emitted by an auto-workflow
		 *     builder; this surfaces the "Run benchmark to optimize" CTA in the dashboard.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description Display name for the workflow. */
						name: string;
						dag: components["schemas"]["DagDefinition"];
						/** @description Optional. Marks the workflow as emitted by an auto-workflow builder so the dashboard can surface the "Run benchmark" CTA. */
						autoGenerated?: boolean;
					};
				};
			};
			responses: {
				/** @description The created workflow. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Workflow"];
					};
				};
				/** @description Missing or invalid `name` or `dag` field, or the DAG failed structural validation. Body includes `validationErrors` when DAG validation failed. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							error?: string;
							validationErrors?: Record<string, never>[];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/create-default-smart": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Create default smart workflow (no benchmark)
		 * @description Create a smart workflow from the org's connected providers without requiring
		 *     benchmark data. Used by the smart-workflow flow when a user has no URL history
		 *     to benchmark (fresh org, or after deleting all workflows including the default),
		 *     and by the guided MCP scrape to stand up a per-domain workflow on first use.
		 *
		 *     Provider order is a static alphabetical fallback by providerId — without
		 *     benchmark data we have no signal to rank by. Per-strategy ranking only kicks
		 *     in once the optimization cycle runs against real benchmark data.
		 *
		 *     `route` selects which workflow this call owns, and changes three behaviours:
		 *
		 *     * `*` (the default) — the org-wide default workflow. Chain is capped at 3
		 *       providers, and an org with no active provider accounts gets a 400.
		 *     * `domain:<host>` — a workflow routed to one domain, resolved ahead of the
		 *       `*` default for URLs on that host. The chain includes **every** supported
		 *       vendor (a vendor the org cannot authenticate simply fails over at runtime),
		 *       and an org with no active provider accounts is **not** an error — a
		 *       platform-managed org scrapes on platform credentials and legitimately has
		 *       none. The host segment is normalized server-side (lowercased, `www.`
		 *       stripped) so it matches the URL's generated `domain:` tag.
		 *
		 *     Idempotent per route: if a workflow already exists on this route for the org,
		 *     it is updated in place; otherwise a new one is created and published.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @enum {string} */
						strategy: "success" | "cost" | "reliability" | "quality";
						/**
						 * @description `*` for the org-wide default (the behaviour when omitted), or
						 *     `domain:<host>` for a domain-routed workflow. The host segment is
						 *     normalized server-side, so `domain:www.example.com` and
						 *     `domain:example.com` address the same workflow.
						 * @default *
						 * @example domain:example.com
						 */
						route?: string;
						pipeline?: {
							includeStorage?: boolean;
							includeExtractor?: boolean;
							extractorPageType?: string;
						};
					};
				};
			};
			responses: {
				/** @description The created or updated default smart workflow. Body is wrapped under `workflow` rather than returned directly so callers can disambiguate create-vs-update via `workflow.isUpdate`. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							workflow?: {
								/** Format: uuid */
								workflowId?: string;
								name?: string;
								/** @description The canonical route this workflow was created on — '*', or the normalized 'domain:<host>'. */
								route?: string;
								providerOrder?: string[];
								/** @description Always null on this endpoint — cost estimation needs benchmark data. */
								costPerThousand?: number | null;
								/** @description true when an existing default workflow was overwritten; false when a fresh one was created. */
								isUpdate?: boolean;
							};
						};
					};
				};
				/** @description Invalid strategy (must be one of `success`, `cost`, `reliability`, `quality`), a malformed `route` (must be `*` or `domain:<host>`), or no active providers configured for the org on the `*` route. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{id}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get workflow
		 * @description Get a workflow (latest version) with its DAG by ID. Returns the full DAG node + edge graph for editor consumption.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description The requested workflow with its DAG. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Workflow"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		/**
		 * Update workflow
		 * @description Update a workflow. Each successful update **creates a new version** rather than
		 *     mutating in place — the previous version stays addressable via
		 *     `GET /api/v1/workflows/{id}/versions/{version}`. Smart workflows are read-only;
		 *     attempting to edit one returns 400 — clone it first via
		 *     `POST /api/v1/workflows/{id}/clone`. The DAG is validated server-side; structural
		 *     errors return 400 with a `validationErrors` array.
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						name: string;
						dag: components["schemas"]["DagDefinition"];
					};
				};
			};
			responses: {
				/** @description The updated workflow header. Returns only `workflowId`, `name`, and the new `version` integer rather than the full Workflow schema — fetch via `GET /api/v1/workflows/{id}` if you need the full DAG echoed back. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							workflowId?: string;
							name?: string;
							/** @description The new version number created by this update. */
							version?: number;
						};
					};
				};
				/** @description Invalid `name`/`dag` field, DAG failed structural validation, or the workflow is a smart workflow that must be cloned before editing. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							error?: string;
							validationErrors?: Record<string, never>[];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		/**
		 * Delete workflow
		 * @description Soft-delete a workflow by ID — the row stays in the database with `status='deleted'`
		 *     so completed jobs that referenced it can still resolve their workflow at audit
		 *     time. The accompanying smart-workflow config (if any) is hard-deleted via
		 *     `deleteSmartWorkflow`. Returns 204 with no body on success.
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Workflow soft-deleted; smart-workflow config (if any) hard-deleted. */
				204: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{id}/clone": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Clone as custom
		 * @description Clone a workflow as a non-smart custom draft. The clone is independent — it has
		 *     its own ID, version history, and routing. Use this when you want to start from
		 *     a smart workflow's DAG but edit it freely (smart workflows themselves are
		 *     read-only and reject `PUT`). The clone starts in `draft` status with no route;
		 *     `POST /api/v1/workflows/{id}/publish` to make it live.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow to clone, by its stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description The newly created clone. */
				201: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Workflow"];
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{id}/validate": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Validate DAG without saving
		 * @description Run the workflow validator against a candidate DAG without persisting it.
		 *     Used by the dashboard editor to surface structural errors live while the user
		 *     is editing. The path `{id}` is required by Express routing but is **not used**
		 *     to look up a row — validation runs purely against the body's `dag` field.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Unused — present only to satisfy Express routing. Pass any non-empty string. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						dag: components["schemas"]["DagDefinition"];
					};
				};
			};
			responses: {
				/**
				 * @description Validation result. `valid` true means the DAG would pass server-side
				 *     checks on save/publish; `valid` false means it would 400. `errors` is an
				 *     array of structured validation errors when `valid` is false.
				 */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							valid?: boolean;
							errors?: Record<string, never>[];
						};
					};
				};
				400: components["responses"]["BadRequest"];
				401: components["responses"]["Unauthorized"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{id}/publish": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Publish workflow
		 * @description Validate the workflow's latest version and flip its status to `published`.
		 *     Once published, the workflow is eligible to receive dispatched scrape jobs
		 *     (resolution still depends on `route`). Returns 400 with `validationErrors`
		 *     if the DAG fails structural validation — re-edit, fix the errors, then retry.
		 *
		 *     **Set the route before you publish.** `POST /api/v1/workflows` creates the
		 *     workflow with `route: "*"` (the org-wide default route), and only ONE
		 *     published workflow per org may hold any given route. So the second workflow
		 *     you ever publish will 409 unless you first give it its own route via
		 *     `PUT /api/v1/workflows/{id}/routing` — or unpublish whichever workflow
		 *     currently holds the one you want.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Workflow status flipped to `published`. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							workflowId?: string;
							/** @enum {string} */
							status?: "published";
						};
					};
				};
				/** @description Workflow DAG has validation errors that prevented publish. Body includes a `validationErrors` array. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							error?: string;
							validationErrors?: Record<string, never>[];
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				/**
				 * @description Another published workflow in this org already holds this workflow's
				 *     route. Give this workflow a different route with
				 *     `PUT /api/v1/workflows/{id}/routing`, or unpublish the one that holds it.
				 */
				409: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							error?: string;
						};
					};
				};
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{id}/unpublish": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Unpublish workflow
		 * @description Revert a published workflow back to `draft` and clear its route. Dispatch
		 *     resolution will skip the workflow until it's re-published with a route.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description Workflow status flipped to `draft`; route cleared. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							workflowId?: string;
							/** @enum {string} */
							status?: "draft";
						};
					};
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{id}/routing": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		/**
		 * Set workflow route
		 * @description Set or clear the workflow's `route` field. The route controls dispatch
		 *     resolution: `'*'` is the org's default workflow, `'domain:example.com'` matches
		 *     URLs on that domain, `null` clears routing (the workflow will only fire on
		 *     explicit `workflowId` dispatches). At most one published workflow per route
		 *     value is allowed at a time (DB unique constraint).
		 */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
				};
				cookie?: never;
			};
			requestBody: {
				content: {
					"application/json": {
						/** @description `*` for default, `domain:X` for a domain match, or `null` to clear. */
						route?: string | null;
					};
				};
			};
			responses: {
				/** @description Route updated. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": {
							/** Format: uuid */
							workflowId?: string;
							route?: string | null;
						};
					};
				};
				/** @description `route` must be a string or null. */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/api/v1/workflows/{id}/versions/{version}": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get a specific workflow version
		 * @description Fetch a frozen prior version of a workflow's DAG. Every `PUT /api/v1/workflows/{id}`
		 *     creates a new version; older versions stay addressable here for audit + diff use
		 *     cases. `GET /api/v1/workflows/{id}` always returns the latest.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description The workflow's stable logical id — the `workflowId` field of a workflow object, NOT its `id` field. `id` is the per-version row key and 404s here. */
					id: string;
					/** @description The version number (1-indexed). */
					version: number;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description The frozen workflow version. */
				200: {
					headers: {
						[name: string]: unknown;
					};
					content: {
						"application/json": components["schemas"]["Workflow"];
					};
				};
				/** @description Invalid version number (must be a positive integer). */
				400: {
					headers: {
						[name: string]: unknown;
					};
					content?: never;
				};
				401: components["responses"]["Unauthorized"];
				403: components["responses"]["Forbidden"];
				404: components["responses"]["NotFound"];
				500: components["responses"]["ServerError"];
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
}
export interface components {
	schemas: {
		Job: {
			/** Format: uuid */
			id?: string;
			/** @enum {string} */
			status?: "pending" | "running" | "completed" | "failed" | "waiting_retry" | "extracting" | "extraction_done";
			url?: string;
			/** Format: uuid */
			workflowId?: string;
			version?: number;
			providerId?: string | null;
			statusCode?: number | null;
			latencyMs?: number | null;
			errorType?: string | null;
			errorMessage?: string | null;
			/** Format: uuid */
			scheduleId?: string | null;
			/** Format: date-time */
			createdAt?: string;
			/** Format: date-time */
			completedAt?: string | null;
		};
		Workflow: {
			/**
			 * Format: uuid
			 * @description Primary key of this specific VERSION row. Changes every time the workflow is edited, and is not accepted by any endpoint. Useful only for correlating a response with the exact version it came from — pass `workflowId`, not this, to `/api/v1/workflows/{id}`.
			 */
			id?: string;
			/**
			 * Format: uuid
			 * @description Stable logical identifier for the workflow, constant across every version. THIS is the value every `/api/v1/workflows/{id}` route expects (get, update, delete, clone, publish, unpublish, routing, versions) and the value jobs and schedules reference.
			 */
			workflowId?: string;
			name?: string;
			/** @enum {string} */
			status?: "draft" | "published";
			version?: number;
			route?: string | null;
			dag?: components["schemas"]["DagDefinition"];
			costPerThousand?: number | null;
			smart?: components["schemas"]["SmartConfig"];
			/** Format: date-time */
			createdAt?: string;
		};
		DagDefinition: {
			nodes: {
				id: string;
				/** @enum {string} */
				type: "entry" | "service" | "split" | "result" | "failed";
				config?: Record<string, never> | null;
			}[];
			edges: {
				id: string;
				from: string;
				to: string;
				/** @enum {string} */
				type: "default" | "success" | "fail" | "split";
			}[];
		};
		SmartConfig: {
			/** @enum {string} */
			strategy?: "success" | "cost" | "reliability";
			/** @enum {string} */
			mode?: "auto" | "suggest";
			domain?: string | null;
			hasPendingSuggestion?: boolean;
		};
		UrlItem: {
			/** Format: uuid */
			id?: string;
			url?: string;
			domain?: string;
			priority?: number;
			tags?: string[];
			/** Format: date-time */
			createdAt?: string;
		};
		Schedule: {
			/** Format: uuid */
			id?: string;
			name?: string;
			/** Format: uuid */
			workflowId?: string | null;
			tagFilters?: string[];
			/** @enum {string} */
			frequency?: "hourly" | "daily" | "weekly" | "monthly" | "custom";
			cronExpr?: string;
			timezone?: string;
			isEnabled?: boolean;
			/** Format: date-time */
			nextRunAt?: string;
			/** Format: date-time */
			lastRunAt?: string | null;
			/** Format: date-time */
			createdAt?: string;
		};
		Benchmark: {
			/** Format: uuid */
			id?: string;
			/** @enum {string} */
			status?: "pending" | "running" | "completed" | "failed";
			urls?: string[];
			providerIds?: string[];
			totalProbes?: number;
			completedProbes?: number;
			failedProbes?: number;
			skippedProviders?: string[];
			error?: string | null;
			/** Format: date-time */
			createdAt?: string;
		};
		Suggestion: {
			/** Format: uuid */
			id?: string;
			/** @enum {string} */
			status?: "pending" | "applied" | "dismissed";
			currentProviderOrder?: string[];
			suggestedProviderOrder?: string[];
			reason?: string;
			improvementPct?: number | null;
			improvementMetric?: string;
			estimatedCostBefore?: number | null;
			estimatedCostAfter?: number | null;
			/** Format: date-time */
			createdAt?: string;
			/** Format: date-time */
			resolvedAt?: string | null;
			/** Format: date-time */
			expiresAt?: string;
		};
		InputQueueConfig: {
			/** Format: uuid */
			id?: string;
			name?: string;
			/** @enum {string} */
			queueType?: "sqs" | "webhook";
			queueUrl?: string | null;
			/** Format: uuid */
			workflowId?: string | null;
			isEnabled?: boolean;
			pollIntervalMs?: number;
			maxUrlsPerDay?: number;
			webhookUrl?: string | null;
			/** Format: date-time */
			lastPolledAt?: string | null;
			lastError?: string | null;
			/** Format: date-time */
			createdAt?: string;
		};
		OutputConfig: {
			/** Format: uuid */
			id?: string;
			name?: string;
			/** @enum {string} */
			outputType?: "s3" | "webhook";
			destination?: string;
			isEnabled?: boolean;
			/** Format: date-time */
			lastDeliveredAt?: string | null;
			lastError?: string | null;
			/** Format: date-time */
			createdAt?: string;
		};
		PlanStatus: {
			/** @enum {string} */
			plan?: "trial" | "paygo" | "enterprise";
			canSubmitJobs?: boolean;
			remainingCredits?: number | null;
			trialDaysLeft?: number | null;
			isTrialExpired?: boolean;
		};
		AttributeSchema: {
			name: string;
			xpaths: string[];
			/** @enum {string} */
			type: "text" | "number" | "url" | "boolean" | "text[]" | "url[]";
			description?: string | null;
			antiPatterns?: string[] | null;
			/** @enum {string|null} */
			semanticType?: "brand_name" | "review_count" | "product_id" | "product_image" | "star_rating" | null;
			validation?: {
				minLength?: number;
				maxLength?: number;
				min?: number;
				max?: number;
				pattern?: string;
			} | null;
		};
		ExtractionSchema: {
			id?: string;
			domain?: string;
			/** @enum {string} */
			pageType?: "product" | "product_list" | "serp" | "article" | "job_posting";
			version?: number;
			attributes?: components["schemas"]["AttributeSchema"][];
			confidence?: number;
			/** @enum {string} */
			discoveredBy?: "llm" | "llm_multi_sample" | "manual" | "reverse_engineered";
			sampleCount?: number;
			/** Format: date-time */
			lastValidatedAt?: string;
			/** Format: date-time */
			expiresAt?: string;
			/** Format: date-time */
			createdAt?: string;
		};
		ExtractionResult: {
			/** Format: uuid */
			id?: string;
			/** Format: uuid */
			orgId?: string;
			/** Format: uuid */
			jobId?: string;
			domain?: string;
			schemaId?: string | null;
			dataType?: string;
			normalized?: {
				[key: string]: unknown;
			};
			/** @description Internal storage key for the raw HTML blob. */
			rawBlobKey?: string | null;
			qualityCompleteness?: number;
			qualityValidation?: number;
			qualityComposite?: number;
			latencyMs?: number | null;
			/** Format: date-time */
			deliveredAt?: string | null;
			/** Format: date-time */
			createdAt?: string;
		};
		/** @description Org-scoped custom attributes. When no row exists, the API returns `{ attributes: [] }` and other fields are omitted. */
		OrgAttributes: {
			/** Format: uuid */
			id?: string;
			/** Format: uuid */
			orgId?: string;
			domain?: string;
			pageType?: string;
			attributes: components["schemas"]["AttributeSchema"][];
			/** Format: date-time */
			createdAt?: string;
			/** Format: date-time */
			updatedAt?: string;
		};
		Error: {
			error?: string;
		};
		/** @description Error envelope returned by every `/api/v1/bi/*` handler error. Unlike the rest of the API — where the structured shape is opt-in via the `application/vnd.justcrawl.v1+json` media type — a BI handler always emits it, and all four fields are always present. The one exception: 401s from missing or invalid auth are rejected by shared middleware before any BI handler runs, so they carry the flat `{ "error": "<message>" }` shape (`Error` schema) instead — see the `Unauthorized` response on each operation. */
		BiError: {
			error: {
				/** @description Stable, machine-readable error code. Match on this, not on `message`. */
				code: string;
				/** @description Customer-safe description. Never raw exception or driver text, with one deliberate exception: a `syntax_error` (SQLSTATE 42601) passes the database's own message through verbatim, because it describes SQL the caller wrote themselves and names nothing they could not already see — see the root `CLAUDE.md` §Error reporting for the full carve-out. */
				message: string;
				/**
				 * Format: uri
				 * @description Reference page for this code. Codes shared with the rest of the API have their own page; the BI-local vocabulary points at the single BI errors page that tabulates all of them.
				 */
				docs_url: string;
				/** @description Correlation id. Identical to the `X-Request-ID` response header — quote either one to support. */
				request_id: string;
			};
		};
		/** @description One row of the submitted-query history (`GET /api/v1/bi/queries`). Routing is transparent: the engine that executed the query is never surfaced. */
		BiQuery: {
			/** Format: uuid */
			id?: string;
			/** Format: uuid */
			userId?: string;
			/** @enum {string} */
			status?: "queued" | "running" | "success" | "failed" | "canceled";
			label?: string | null;
			sql?: string;
			rowCount?: number | null;
			resultByteSize?: number | null;
			errorCode?: string | null;
			/** Format: date-time */
			submittedAt?: string;
			/** Format: date-time */
			startedAt?: string | null;
			/** Format: date-time */
			finishedAt?: string | null;
		};
		/** @description Chart picker selection shape. `bar` and `line` accept multiple y series; `pie` collapses to a single one. */
		ChartConfig: {
			/** @enum {string} */
			type: "bar" | "line" | "pie";
			x: string;
			y: string | string[];
		};
		/** @description A saved BI query. Returned by the single-row routes (get, create, patch, and the share mint/revoke pair). List rows drop `shareToken` and add `isShared` plus `scheduleCount` instead. */
		BiSavedQuery: {
			/** Format: uuid */
			id?: string;
			/** Format: uuid */
			userId?: string;
			name?: string;
			sql?: string;
			description?: string | null;
			/**
			 * Format: password
			 * @description 192-bit bearer capability granting **unauthenticated** read of this query’s results via `/api/v1/share/sql/{token}` — outside authentication and outside the BI feature flag. Never log it, never persist it outside your own secret store, and never include it in an agent transcript. Revoked via `DELETE /api/v1/bi/saved-queries/{id}/share` — a working route that, like this mint endpoint, is deliberately withheld from this contract rather than dashboard-only. Single-row responses only; the list route returns `isShared` instead.
			 */
			shareToken?: string | null;
			chartConfig?: components["schemas"]["ChartConfig"] | null;
			/** Format: date-time */
			createdAt?: string;
			/** Format: date-time */
			updatedAt?: string;
		};
		/** @description One page of result rows. Returned **bare** by `GET /api/v1/bi/queries/{id}/results`, but **nested under `results`** by `POST /api/v1/bi/queries` when the sync-wait window catches a completed query. The two are not interchangeable. */
		BiQueryResults: {
			rows: {
				[key: string]: unknown;
			}[];
			page: number;
			pageSize: number;
			/** @description Total rows materialized for this query. `-1` when unknown. */
			totalRows: number;
			hasMore: boolean;
		};
		/** @description A CSV or Parquet export of a completed query. Exports are materialized asynchronously. */
		BiQueryExport: {
			/** Format: uuid */
			id?: string;
			/** Format: uuid */
			queryId?: string;
			/** @enum {string} */
			format?: "csv" | "parquet";
			/** @enum {string} */
			status?: "queued" | "running" | "ready" | "failed";
			rowCount?: number | null;
			byteSize?: number | null;
			/** @description Machine-readable failure reason, present only when `status` is `failed`. One of: `source_read_failed` (reading the source query's results failed — includes a provider-side download error, or the stored results were corrupted), `upload_failed` (writing the export to storage failed), `encoding_error` (the encoder itself rejected the row shape), or a BI query-engine code (`syntax_error`, `statement_timeout`, `rls_denied`, `too_many_queries`, `kill_switched`, `canceled`, `transient`) when the source query became invalid partway through the export. */
			errorCode?: string | null;
			/** @description Customer-safe description of the failure, present only when `status` is `failed`. Never raw exception, driver, or cloud-provider text — see `errorCode` for the machine-readable reason. */
			errorMessage?: string | null;
			/** Format: date-time */
			requestedAt?: string;
			/** Format: date-time */
			startedAt?: string | null;
			/** Format: date-time */
			completedAt?: string | null;
			/**
			 * Format: uri
			 * @description Presigned S3 credential granting read access to the export bytes with no authentication. Expires after `downloadUrlExpiresInSec` and is re-issued on every poll, so fetch it when you need it — never log, cache, or forward it. Null until `status` is `ready`.
			 */
			downloadUrl?: string | null;
			/** @description Lifetime of `downloadUrl` in seconds. Null whenever `downloadUrl` is null. */
			downloadUrlExpiresInSec?: number | null;
		};
	};
	responses: {
		/** @description Missing or invalid authentication token */
		Unauthorized: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": "Missing or invalid authentication token"
				 *     }
				 */
				"application/json": components["schemas"]["Error"];
			};
		};
		/** @description Insufficient permissions for this operation */
		Forbidden: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": "No organization. Complete onboarding first."
				 *     }
				 */
				"application/json": components["schemas"]["Error"];
			};
		};
		/** @description Resource not found */
		NotFound: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": "Workflow not found"
				 *     }
				 */
				"application/json": components["schemas"]["Error"];
			};
		};
		/** @description Validation failed — request body or query is missing/invalid */
		BadRequest: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": "Missing or invalid \"url\" field"
				 *     }
				 */
				"application/json": components["schemas"]["Error"];
			};
		};
		/** @description Operation conflicts with current resource state */
		Conflict: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": "Resource already exists"
				 *     }
				 */
				"application/json": components["schemas"]["Error"];
			};
		};
		/** @description Unexpected server error. Logs and PostHog `$exception` capture */
		ServerError: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": "Something went wrong"
				 *     }
				 */
				"application/json": components["schemas"]["Error"];
			};
		};
		/** @description Access to the BI surface itself is refused. `no_org` and `feature_disabled` fire on every BI route from the mount guard, before any handler runs — `feature_disabled` is the first response most new integrations see, because the SQL console is entitled per organization. `rls_denied` and `kill_switched` come from the engine. This response never describes resource-level authorization: an id you do not own returns 404, not 403. */
		BiForbidden: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": {
				 *         "code": "feature_disabled",
				 *         "message": "SQL console is not enabled for this organization",
				 *         "docs_url": "https://docs.justcrawl.io/guides/errors/bi",
				 *         "request_id": "req_9f3a1c7e2b40"
				 *       }
				 *     }
				 */
				"application/json": components["schemas"]["BiError"] & {
					error?: {
						/** @enum {string} */
						code?: "no_org" | "feature_disabled" | "rls_denied" | "kill_switched";
					};
				};
			};
		};
		/** @description Not found, or not owned by your organization. The two are deliberately indistinguishable so cross-tenant existence cannot be probed */
		BiNotFound: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": {
				 *         "code": "not_found",
				 *         "message": "Query not found",
				 *         "docs_url": "https://docs.justcrawl.io/guides/errors/not_found",
				 *         "request_id": "req_9f3a1c7e2b40"
				 *       }
				 *     }
				 */
				"application/json": components["schemas"]["BiError"];
			};
		};
		/** @description Unexpected server error. Full detail goes to the logs and PostHog `$exception` capture, never the response */
		BiServerError: {
			headers: {
				[name: string]: unknown;
			};
			content: {
				/**
				 * @example {
				 *       "error": {
				 *         "code": "internal_error",
				 *         "message": "Failed to list queries",
				 *         "docs_url": "https://docs.justcrawl.io/guides/errors/internal_error",
				 *         "request_id": "req_9f3a1c7e2b40"
				 *       }
				 *     }
				 */
				"application/json": components["schemas"]["BiError"];
			};
		};
	};
	parameters: never;
	requestBodies: never;
	headers: never;
	pathItems: never;
}
/** Every path the published spec documents. */
export type ApiPath = keyof paths;
/** HTTP methods that can appear on a path item. */
export type HttpMethod = "get" | "post" | "put" | "patch" | "delete";
/**
 * The methods actually defined on `P`.
 *
 * `openapi-typescript` emits absent methods as `never`, not as missing keys, so
 * a plain `keyof paths[P]` would offer every verb on every path. Filtering on
 * `never` is what makes `client.request('put', '/api/v1/jobs')` a compile error.
 */
export type MethodOf<P extends ApiPath> = {
	[M in HttpMethod]: M extends keyof paths[P] ? (paths[P][M] extends undefined ? never : M) : never;
}[HttpMethod];
/** The operation object for one path+method pair. */
export type Operation<P extends ApiPath, M extends MethodOf<P>> = paths[P][M & keyof paths[P]];
/** Unwrap `{ content: { 'application/json': B } }` to `B`. */
export type JsonBody<T> = T extends {
	content: {
		"application/json": infer B;
	};
} ? B : never;
/**
 * The success body for an operation.
 *
 * A union over the 2xx codes the operation declares, because several endpoints
 * legitimately answer with more than one — `POST /extraction/backfill` returns
 * `202`, the five wrapped DELETEs return `204` (modeled as `undefined`, which is
 * exactly what the transport resolves for an empty body).
 */
export type SuccessBody<O> = (O extends {
	responses: {
		200: infer R;
	};
} ? JsonBody<R> : never) | (O extends {
	responses: {
		201: infer R;
	};
} ? JsonBody<R> : never) | (O extends {
	responses: {
		202: infer R;
	};
} ? JsonBody<R> : never) | (O extends {
	responses: {
		204: unknown;
	};
} ? undefined : never);
/** The success body for a path+method pair. */
export type ResultOf<P extends ApiPath, M extends MethodOf<P>> = SuccessBody<Operation<P, M>>;
/** The JSON request body a path+method pair accepts, or `never` when it takes none. */
export type BodyOf<P extends ApiPath, M extends MethodOf<P>> = Operation<P, M> extends {
	requestBody?: infer RB;
} ? JsonBody<NonNullable<RB>> : never;
/** The query parameters a path+method pair accepts, or `never` when it takes none. */
export type QueryOf<P extends ApiPath, M extends MethodOf<P>> = Operation<P, M> extends {
	parameters: {
		query?: infer Q;
	};
} ? NonNullable<Q> : never;
/**
 * The path parameters a path+method pair declares, or `never` when it has none.
 *
 * This is what lets `client.request()` take the spec's own template —
 * `'/api/v1/jobs/{id}'` — rather than a pre-interpolated string. A template is a
 * member of the `ApiPath` union and so is checked against the spec; an
 * interpolated `` `/api/v1/jobs/${id}` `` is just `string` and is checked
 * against nothing. `openapi-typescript` emits absent path params as
 * `path?: never`, which `NonNullable` collapses back to `never` — so
 * "has params" is a decidable question at the type level, and `TypedRequestOptions`
 * uses it to make `path` required exactly when the endpoint needs it.
 */
export type PathParamsOf<P extends ApiPath, M extends MethodOf<P>> = Operation<P, M> extends {
	parameters: {
		path?: infer PP;
	};
} ? NonNullable<PP> : never;
export type SchemaMap = components["schemas"];
/** Names of every schema in `components.schemas`. */
export type SchemaName = keyof SchemaMap;
/** A named schema from the spec's `components.schemas`. */
export type Schema<N extends SchemaName> = SchemaMap[N];
/** A scrape job, exactly as the spec defines it. */
export type Job = SchemaMap["Job"];
/**
 * Job lifecycle statuses, split terminal vs not.
 *
 * The polling helpers key off these, so they live here next to the generated
 * `Job` type rather than in `polling.ts` — if the spec's status enum grows a
 * value, the mismatch surfaces as a type error at the assertion below instead of
 * as a poll loop that never terminates.
 */
export declare const TERMINAL_JOB_STATUSES: readonly [
	"completed",
	"failed"
];
export declare const NON_TERMINAL_JOB_STATUSES: readonly [
	"pending",
	"running",
	"waiting_retry",
	"extracting",
	"extraction_done"
];
export type TerminalJobStatus = (typeof TERMINAL_JOB_STATUSES)[number];
export type NonTerminalJobStatus = (typeof NON_TERMINAL_JOB_STATUSES)[number];
export type JobStatus = TerminalJobStatus | NonTerminalJobStatus;
/** Whether a status means the job will not change again. */
export declare function isTerminalStatus(status: string): status is TerminalJobStatus;
declare abstract class Resource {
	protected readonly client: JustCrawl;
	constructor(client: JustCrawl);
	/**
	 * Narrow a generated query-parameter object to the transport's loose
	 * `QueryParams`.
	 *
	 * The generated types describe each endpoint's parameters precisely (literal
	 * unions for enums, `number` for pages); `buildQueryString` accepts the general
	 * shape. The two are structurally compatible but TypeScript will not infer the
	 * widening across an index signature, so the cast happens once here rather than
	 * ~99 times at the call sites — and stays visible as a single reviewable seam.
	 */
	protected q(query: unknown): QueryParams | undefined;
	/**
	 * Forward per-call options (`signal`, `timeoutMs`, `headers`) without repeating
	 * the spread.
	 *
	 * Generic in `extra` on purpose. `request()`'s options type is now narrowed per
	 * operation — `path` is required for a templated endpoint and `path?: never`
	 * for a flat one — so a helper returning the loose `RequestOptions` would be
	 * rejected at every one of the ~101 call sites. Preserving `extra`'s literal
	 * type lets the narrowed shape survive the merge.
	 */
	protected opts<T extends object>(options: CallOptions | undefined, extra: T): CallOptions & T;
}
/** Options every wrapper method accepts on top of its own arguments. */
export type CallOptions = Pick<RequestOptions, "signal" | "timeoutMs" | "headers">;
export declare class AnalyticsResource extends Resource {
	/** Headline counters: jobs, success rate, spend. */
	overview(query?: QueryOf<"/api/v1/analytics/overview", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/analytics/overview", "get">>;
	/** Bucketed series over the window; `interval` picks the bucket width. */
	timeseries(query?: QueryOf<"/api/v1/analytics/timeseries", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/analytics/timeseries", "get">>;
	/** Per-provider success rate, latency, and cost. */
	providers(query?: QueryOf<"/api/v1/analytics/providers", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/analytics/providers", "get">>;
	/** Top domains by volume. */
	domains(query?: QueryOf<"/api/v1/analytics/domains", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/analytics/domains", "get">>;
	/** The domain × provider grid — which vendor actually works where. */
	matrix(query?: QueryOf<"/api/v1/analytics/matrix", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/analytics/matrix", "get">>;
}
export declare class BenchmarksResource extends Resource {
	/**
	 * Start a benchmark run.
	 *
	 * Benchmarks scrape for real, so this spends provider quota. It also `503`s
	 * rather than charging when the platform credentials for a provider are unset.
	 */
	create(body: BodyOf<"/api/v1/benchmarks", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/benchmarks", "post">>;
	/** The most recent completed benchmark for the org. */
	latest(options?: CallOptions): Promise<ResultOf<"/api/v1/benchmarks/latest", "get">>;
	get(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/benchmarks/{id}", "get">>;
	/** Stop a run in flight — the cheapest way to cap a benchmark's spend. */
	cancel(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/benchmarks/{id}/cancel", "post">>;
	results(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/benchmarks/{id}/results", "get">>;
}
export declare class BiResource extends Resource {
	/** The queryable schema: tables and columns available to this org. */
	getSchema(options?: CallOptions): Promise<ResultOf<"/api/v1/bi/schema", "get">>;
	/** Column detail for one table. */
	getTable(name: string, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/tables/{name}", "get">>;
	/**
	 * Submit a SQL query. Answers `429` (`too_many_queries`) when the org already
	 * has too many in flight — a GET would be retried, but this POST is not.
	 */
	runQuery(body: BodyOf<"/api/v1/bi/queries", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/queries", "post">>;
	/** Recent query runs. */
	listQueries(query?: QueryOf<"/api/v1/bi/queries", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/queries", "get">>;
	/** One query run: status, timing, and error detail if it failed. */
	getQuery(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/queries/{id}", "get">>;
	/** Row count, column list, and page count — cheaper than fetching page 1. */
	getResultManifest(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/queries/{id}/result-manifest", "get">>;
	/** One page of results. `pageSize` caps at 100 — pages, never a stream. */
	getResults(id: string, query?: QueryOf<"/api/v1/bi/queries/{id}/results", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/queries/{id}/results", "get">>;
	cancelQuery(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/queries/{id}/cancel", "post">>;
	listSavedQueries(options?: CallOptions): Promise<ResultOf<"/api/v1/bi/saved-queries", "get">>;
	createSavedQuery(body: BodyOf<"/api/v1/bi/saved-queries", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/saved-queries", "post">>;
	/** One saved query, including its `sql` — the first half of "run a saved query". */
	getSavedQuery(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/saved-queries/{id}", "get">>;
	updateSavedQuery(id: string, body: BodyOf<"/api/v1/bi/saved-queries/{id}", "patch">, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/saved-queries/{id}", "patch">>;
	deleteSavedQuery(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/saved-queries/{id}", "delete">>;
	/** Start an export of a completed query's results. */
	createExport(queryId: string, body: BodyOf<"/api/v1/bi/queries/{id}/exports", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/queries/{id}/exports", "post">>;
	/** Export status and, once ready, its download pointer. */
	getExport(exportId: string, options?: CallOptions): Promise<ResultOf<"/api/v1/bi/exports/{exportId}", "get">>;
}
export declare class ExtractionResource extends Resource {
	/** Recent extraction results across the org, optionally filtered by domain. */
	listResults(query?: QueryOf<"/api/v1/extraction/results", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/results", "get">>;
	/**
	 * The extraction result for one job.
	 *
	 * **A `202` here is not a result.** When the job is in `extraction_done` but
	 * the bulk-writer has not flushed yet, the API answers `202` with an
	 * `indexingPending` body and a `Retry-After: 5` header. `polling.ts` models
	 * that as its own outcome; this method returns the body as the spec types it,
	 * so check before treating it as an `ExtractionResult`.
	 */
	getResultByJob(jobId: string, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/results/{jobId}", "get">>;
	/** One extraction result by its own id (not the job's). */
	getResultById(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/results/by-id/{id}", "get">>;
	/** The raw captured HTML behind an extraction result. */
	getRawResult(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/results/{id}/raw", "get">>;
	/** Discovered XPath schemas, filterable by domain and page type. */
	listSchemas(query?: QueryOf<"/api/v1/extraction/schemas", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/schemas", "get">>;
	/**
	 * One domain's schema for a page type.
	 *
	 * `pageType` is the spec's own enum rather than a bare `string`, derived here
	 * so it cannot drift: it was `string` until the typed `request()` refused it,
	 * which meant a caller could ask for a page type the API has never had and
	 * find out only from the 404.
	 */
	getSchema(domain: PathParamsOf<"/api/v1/extraction/schemas/{domain}/{pageType}", "get">["domain"], pageType: PathParamsOf<"/api/v1/extraction/schemas/{domain}/{pageType}", "get">["pageType"], options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/schemas/{domain}/{pageType}", "get">>;
	/** Custom attributes configured for a domain. */
	getAttributes(domain: string, query?: QueryOf<"/api/v1/extraction/attributes/{domain}", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/attributes/{domain}", "get">>;
	/** Replace a domain's custom attributes wholesale. */
	setAttributes(domain: string, body: BodyOf<"/api/v1/extraction/attributes/{domain}", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/attributes/{domain}", "put">>;
	deleteAttributes(domain: string, query?: QueryOf<"/api/v1/extraction/attributes/{domain}", "delete">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/attributes/{domain}", "delete">>;
	/** Try an XPath against captured HTML without running a job. */
	testXPath(body: BodyOf<"/api/v1/extraction/test-xpath", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/test-xpath", "post">>;
	/**
	 * Re-extract from already-captured HTML.
	 *
	 * Cheap relative to re-scraping — but bounded by blob retention: keys past the
	 * org's `document_expiry_days` come back `blob_expired`, not re-fetched.
	 */
	backfill(body: BodyOf<"/api/v1/extraction/backfill", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/backfill", "post">>;
	/**
	 * Ensure a schema covering the named attributes exists for a scraped job's
	 * domain, then re-extract that job through it.
	 *
	 * Returns `202` immediately — the work is asynchronous. Poll
	 * {@link getSchema} for the schema and {@link getResultByJob} for this job's
	 * values (`waitForExtractionResult` in `polling.ts` does the latter).
	 *
	 * Like {@link backfill}, this reads the job's cached HTML and never re-hits
	 * the provider, so it is bounded by blob retention: a job whose body is past
	 * the org's `document_expiry_days` answers `410` rather than re-scraping.
	 */
	discover(body: BodyOf<"/api/v1/extraction/discover", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/discover", "post">>;
	/** Extraction history for one URL item. */
	listByUrlItem(urlItemId: string, query?: QueryOf<"/api/v1/extraction/urls/{urlItemId}/extractions", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/extraction/urls/{urlItemId}/extractions", "get">>;
}
export declare class IntegrationsResource extends Resource {
	listInputs(options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/inputs", "get">>;
	createInput(body: BodyOf<"/api/v1/integrations/inputs", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/inputs", "post">>;
	updateInput(id: string, body: BodyOf<"/api/v1/integrations/inputs/{id}", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/inputs/{id}", "put">>;
	deleteInput(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/inputs/{id}", "delete">>;
	toggleInput(id: string, body: BodyOf<"/api/v1/integrations/inputs/{id}/toggle", "patch">, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/inputs/{id}/toggle", "patch">>;
	listOutputs(options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/outputs", "get">>;
	createOutput(body: BodyOf<"/api/v1/integrations/outputs", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/outputs", "post">>;
	/** Platform-managed outputs — readable, not editable by the customer. */
	listInternalOutputs(options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/outputs/internal", "get">>;
	updateOutput(id: string, body: BodyOf<"/api/v1/integrations/outputs/{id}", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/outputs/{id}", "put">>;
	deleteOutput(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/outputs/{id}", "delete">>;
	toggleOutput(id: string, body: BodyOf<"/api/v1/integrations/outputs/{id}/toggle", "patch">, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/outputs/{id}/toggle", "patch">>;
	/** The org's blob storage configuration (bucket, prefix, retention). */
	getStorage(options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/storage", "get">>;
	setStorage(body: BodyOf<"/api/v1/integrations/storage", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/integrations/storage", "put">>;
}
/**
 * Closed enum of customer-visible error codes. New codes are added here;
 * server code throws `HttpError` with the code and the middleware looks
 * up the static message + status from the tables below.
 *
 * Codes are stable contract — renaming a code is a customer-breaking
 * change and requires a deprecation cycle. Adding a new code is fine.
 */
export type JustcrawlErrorCode = "auth_missing" | "auth_invalid" | "auth_forbidden" | "auth_org_mismatch" | "invalid_url" | "invalid_workflow" | "invalid_input" | "node_type_unknown" | "not_found" | "workflow_disabled" | "org_not_found" | "quota_exceeded" | "plan_required" | "payment_required" | "conflict" | "already_exists" | "job_terminal" | "auth_code_expired" | "auth_code_consumed" | "provider_unavailable" | "internal_error" | "service_unavailable";
/**
 * The `code` on a thrown error. Widened past the server's closed enum on
 * purpose: when no code is on the wire we infer one from the status, and two
 * useful inferences (`rate_limited`, `timeout`, `network_error`) have no member
 * in the server vocabulary at all. `inferredCode` tells the two apart — never
 * branch on a code without checking it.
 */
export type JustCrawlErrorCode = JustcrawlErrorCode | "rate_limited" | "timeout" | "network_error";
/** Which of the four wire shapes this error was decoded from. */
export type ErrorWireShape = "structured" | "flat" | "quota" | "non-json" | "transport";
export interface JustCrawlErrorInit {
	message: string;
	status: number;
	code: JustCrawlErrorCode;
	/** True when `code` was inferred from the HTTP status, not read off the wire. */
	inferredCode: boolean;
	shape: ErrorWireShape;
	requestId?: string;
	/** The response body exactly as received — parsed JSON, or a snippet of text. */
	raw?: unknown;
	docsUrl?: string;
	param?: string;
	retryAfterSeconds?: number;
	/**
	 * Whether retrying this exact request could succeed.
	 *
	 * Set by the client from the same predicates the retry loop uses, so a caller
	 * (or an agent) never has to re-derive the SDK's retry table by hand and watch
	 * it drift. Left `undefined` when the SDK cannot know — an error constructed
	 * outside a request, for instance.
	 */
	retryable?: boolean;
	remainingCredits?: number;
	isTrialExpired?: boolean;
	plan?: string;
}
/**
 * Every failure the SDK throws.
 *
 * Note the capital C: this is the SDK's own class, deliberately distinct from
 * the server-side `JustcrawlError` *wire type* it decodes. It **never** carries
 * request headers or the client config — that is what keeps the customer's API
 * key out of logs, crash reporters, and bug reports (R10).
 */
export declare class JustCrawlError extends Error {
	readonly status: number;
	readonly code: JustCrawlErrorCode;
	readonly inferredCode: boolean;
	readonly shape: ErrorWireShape;
	readonly requestId?: string;
	readonly raw?: unknown;
	readonly docsUrl?: string;
	readonly param?: string;
	readonly retryAfterSeconds?: number;
	/**
	 * Whether retrying this exact request could succeed — `false` on a POST (no
	 * `Idempotency-Key` exists, so a retry charges a second credit), `false` on a
	 * 402, `true` on a 429/5xx/network failure to a GET.
	 *
	 * Read this instead of switching on `status` yourself: the SDK's own retry
	 * loop is driven by the same predicates, so the two cannot disagree.
	 */
	readonly retryable?: boolean;
	/** Set only on the 402 quota shape. */
	readonly remainingCredits?: number;
	readonly isTrialExpired?: boolean;
	readonly plan?: string;
	constructor(init: JustCrawlErrorInit);
}
/** The request exceeded `timeoutMs`. A subclass so `catch` blocks can single it out. */
export declare class JustCrawlTimeoutError extends JustCrawlError {
	constructor(timeoutMs: number, requestId?: string, retryable?: boolean);
}
/** The request never got a response (DNS, TCP, TLS, offline). */
export declare class JustCrawlConnectionError extends JustCrawlError {
	constructor(cause: unknown, retryable?: boolean);
}
/** The caller aborted via their own `AbortSignal`. Never retried, never wrapped as a failure. */
export declare class JustCrawlAbortError extends JustCrawlError {
	constructor();
}
/**
 * Best-effort code for a status with no code on the wire.
 *
 * Deliberately conservative — where a status maps to several plausible codes
 * (400 could be `invalid_url`, `invalid_workflow`, `node_type_unknown`…) it
 * picks the most general one rather than guessing a specific failure the server
 * never claimed. Everything here is returned with `inferredCode: true`.
 */
export declare function inferCodeFromStatus(status: number): JustCrawlErrorCode;
export interface PollOptions {
	/** Give up after this long. Defaults to 5 minutes. */
	maxWaitMs?: number;
	/** First gap between polls. Defaults to 1s, then backs off. */
	initialIntervalMs?: number;
	/** Ceiling on the gap between polls. Defaults to 15s. */
	maxIntervalMs?: number;
	/** Caller cancellation. Checked before every poll and during every wait. */
	signal?: AbortSignal;
	/** Called after each poll — useful for progress logging. */
	onPoll?: (job: Job, elapsedMs: number) => void;
	/**
	 * Per-poll request timeout. Defaults to the client's.
	 *
	 * `submitAndWait` takes `PollOptions & CallOptions`, so a caller could always
	 * PASS this — it just had nowhere to land, and every poll silently used the
	 * client default instead.
	 */
	timeoutMs?: number;
	/** Extra headers on each poll request. Same story as `timeoutMs`. */
	headers?: Record<string, string>;
}
/** Thrown when `maxWaitMs` elapses. Carries the last status actually seen. */
export declare class JobWaitTimeoutError extends JustCrawlError {
	readonly jobId: string;
	readonly lastStatus: JobStatus | undefined;
	readonly elapsedMs: number;
	constructor(jobId: string, lastStatus: JobStatus | undefined, elapsedMs: number);
}
/**
 * Poll a job until it reaches `completed` or `failed`.
 *
 * Backoff doubles from `initialIntervalMs` to `maxIntervalMs`, so a job that
 * takes minutes costs a handful of requests rather than one per second.
 *
 * @throws {JobWaitTimeoutError} when `maxWaitMs` elapses first.
 * @throws {JustCrawlAbortError} when the caller's signal fires.
 */
export declare function waitForJob(client: JustCrawl, jobId: string, options?: PollOptions): Promise<Job>;
/** Where a finished job's body actually lives. */
export type JobResultOutcome = {
	/** Platform storage: the body was fetched from a presigned URL. */
	kind: "url";
	data: string;
	providerId: string;
	statusCode: number;
	bodySize: number;
	latencyMs: number;
} | {
	/**
	 * Custom S3: the caller owns the bucket, so the SDK hands back the key
	 * rather than pretending it can read it. Fetch it with your own creds.
	 */
	kind: "blob";
	blobKey: string;
	providerId: string;
	statusCode: number;
	bodySize: number;
	latencyMs: number;
} | {
	/** The result existed but aged out of the org's retention window. */
	kind: "expired";
	expiredAt?: string;
};
/**
 * Fetch a completed job's result, resolving the presigned URL when there is one.
 *
 * A `410 Gone` is returned as `{ kind: 'expired' }` rather than thrown: result
 * expiry is a normal outcome under a short retention policy, not an error the
 * caller did anything to cause. Every other failure still throws.
 *
 * @param fetchImpl Injectable fetch for the presigned URL. Defaults to the
 *   global — deliberately **not** the client's, so no client-level header
 *   configuration can ever leak onto the storage host.
 *
 * `timeoutMs` and `headers` apply to the **pointer request only**. The presigned
 * download is bounded by `timeoutMs` as well, but never receives `headers` —
 * see the fetch below for why that asymmetry is deliberate rather than an
 * oversight.
 */
export declare function fetchJobResult(client: JustCrawl, jobId: string, options?: {
	signal?: AbortSignal;
	timeoutMs?: number;
	headers?: Record<string, string>;
	fetchImpl?: typeof globalThis.fetch;
}): Promise<JobResultOutcome>;
/** The `202 indexingPending` body — a distinct outcome, never an ExtractionResult. */
export interface ExtractionIndexingPending {
	indexingPending: true;
	status: string;
	retryAfterSeconds: number;
	message?: string;
}
/** Whether an extraction response is the "still indexing" 202 rather than a result. */
export declare function isIndexingPending(value: unknown): value is ExtractionIndexingPending;
/**
 * Poll `GET /extraction/results/{jobId}` until the bulk-writer has flushed.
 *
 * The gateway answers `202` with `{ indexingPending: true, retryAfterSeconds }`
 * while a job sits in `extraction_done` — extraction finished, but the row is not
 * queryable yet. That is a *wait*, not a result and not an error, so this helper
 * respects the server's own `retryAfterSeconds` rather than imposing its own
 * backoff: the server knows its flush cadence.
 *
 * @throws {JobWaitTimeoutError} when `maxWaitMs` elapses while still pending.
 */
export declare function waitForExtractionResult<T = unknown>(client: JustCrawl, jobId: string, options?: PollOptions): Promise<T>;
export declare class JobsResource extends Resource {
	/**
	 * Paginated list of jobs in the org.
	 *
	 * Heavy fields (`executionTrace`, `nodeState`) are stripped from list
	 * responses — call {@link get} for those.
	 */
	list(query?: QueryOf<"/api/v1/jobs", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/jobs", "get">>;
	/**
	 * Submit a scrape job. Returns `201` immediately with `status: pending`.
	 *
	 * **Never retried** — there is no idempotency key and a second submission
	 * charges a second credit. Use {@link JustCrawl.jobs.submitAndWait} to poll to
	 * completion.
	 */
	submit(body: BodyOf<"/api/v1/jobs", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/jobs", "post">>;
	/** Status for many job ids in one call — cheaper than N polls. */
	batchStatus(body: BodyOf<"/api/v1/jobs/batch-status", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/jobs/batch-status", "post">>;
	/** Aggregate counters over a rolling window. */
	stats(query?: QueryOf<"/api/v1/jobs/stats", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/jobs/stats", "get">>;
	/** One job, including the heavy trace fields the list endpoint strips. */
	get(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/jobs/{id}", "get">>;
	/**
	 * The job's result pointer.
	 *
	 * Returns exactly one of `resultUrl` (presigned, ~15min TTL) or `blobKey`, and
	 * `410 Gone` once the org's retention window has passed. `polling.ts` wraps
	 * this into typed outcomes — prefer `jobs.fetchResult()` unless you want the
	 * raw pointer.
	 */
	getResultPointer(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/jobs/{id}/result", "get">>;
	/**
	 * Submit a job and poll until it reaches `completed` or `failed`.
	 *
	 * Polling in chunks, never streaming — the API has no long-poll surface.
	 * Honors `signal` and `maxWaitMs`; the submit itself is a POST and so is never
	 * retried, even if the first attempt times out.
	 *
	 * Note this resolves on `failed` too — a job that failed is a *result*, not an
	 * exception. Check `job.status` rather than assuming success.
	 *
	 * @throws {JobWaitTimeoutError} when the job is still running at `maxWaitMs`.
	 */
	submitAndWait(body: BodyOf<"/api/v1/jobs", "post">, options?: PollOptions & CallOptions): Promise<Job>;
	/**
	 * Fetch a finished job's body, resolving the presigned URL when there is one.
	 *
	 * Returns a discriminated outcome rather than a bare string: platform storage
	 * yields `kind: 'url'` with the body, custom S3 yields `kind: 'blob'` with the
	 * key for you to fetch with your own credentials, and an aged-out result yields
	 * `kind: 'expired'` instead of throwing.
	 *
	 * **Your API key is never sent to the storage host** — the presigned URL is
	 * fetched header-free.
	 */
	fetchResult(id: string, options?: CallOptions): Promise<JobResultOutcome>;
}
export declare class PlansResource extends Resource {
	/**
	 * Current plan and remaining credits.
	 *
	 * Worth calling before a large batch: the same numbers drive the `402`
	 * quota gate, and a pre-flight check turns a mid-batch failure into a decision.
	 */
	status(options?: CallOptions): Promise<ResultOf<"/api/v1/plans/status", "get">>;
	/** Credit ledger, newest first. */
	transactions(query?: QueryOf<"/api/v1/plans/transactions", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/plans/transactions", "get">>;
	/** Whether a recharge request is already pending. */
	rechargeRequestStatus(options?: CallOptions): Promise<ResultOf<"/api/v1/plans/recharge-request/status", "get">>;
	/** Ask an administrator for more credits. Does not itself add any. */
	requestRecharge(body: BodyOf<"/api/v1/plans/recharge-request", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/plans/recharge-request", "post">>;
}
export declare class SchedulesResource extends Resource {
	list(query?: QueryOf<"/api/v1/schedules", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules", "get">>;
	/** Create a schedule. Over-quota orgs get 402 with the standard quota payload. */
	create(body: BodyOf<"/api/v1/schedules", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules", "post">>;
	/** Past runs across schedules — the audit trail for "did my crawl fire?". */
	listRuns(query?: QueryOf<"/api/v1/schedules/runs", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules/runs", "get">>;
	get(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules/{id}", "get">>;
	update(id: string, body: BodyOf<"/api/v1/schedules/{id}", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules/{id}", "put">>;
	delete(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules/{id}", "delete">>;
	/** Pause or resume without losing the schedule's configuration or history. */
	toggle(id: string, body: BodyOf<"/api/v1/schedules/{id}/toggle", "patch">, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules/{id}/toggle", "patch">>;
	/** Run a schedule immediately, out of band. Consumes quota like any other run. */
	trigger(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/schedules/{id}/trigger", "post">>;
}
/**
 * Every `workflowId` here is a workflow's STABLE LOGICAL id — the `workflowId`
 * field of a workflow object, never its `id` (the version-row key, which 404s).
 * The parameter name matches the field name for exactly that reason; see
 * {@link WorkflowsResource} for the full explanation.
 *
 * Note that {@link listSuggestions} is the one method that does not 404 on a
 * wrong id — the endpoint returns an empty array for any workflow with no
 * suggestions, so an unrecognized id is indistinguishable from a quiet one.
 */
export declare class SmartWorkflowsResource extends Resource {
	/** Smart state for one workflow: current mode plus optimization telemetry. */
	get(workflowId: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{workflowId}/smart", "get">>;
	/** Switch the workflow between manual and auto-optimizing routing. */
	setMode(workflowId: string, body: BodyOf<"/api/v1/workflows/{workflowId}/smart/mode", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{workflowId}/smart/mode", "put">>;
	/** Pending routing suggestions for one workflow. */
	/**
	 * Every suggestion for one workflow, of every status, newest first.
	 *
	 * Returns `[]` rather than 404 when the workflow has no suggestions — and,
	 * because the endpoint does not verify the id exists, also for an id that
	 * matches nothing. An empty result is not evidence the id was right.
	 */
	listSuggestions(workflowId: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{workflowId}/smart/suggestions", "get">>;
	/** Apply a suggestion — this edits the workflow's routing. */
	applySuggestion(workflowId: string, suggestionId: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{workflowId}/smart/suggestions/{id}/apply", "post">>;
	dismissSuggestion(workflowId: string, suggestionId: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{workflowId}/smart/suggestions/{id}/dismiss", "post">>;
	/**
	 * Suggestions across every workflow in the org.
	 *
	 * Lives on this group rather than `workflows` because the payload is the smart
	 * suggestion shape, even though its path (`/api/v1/suggestions`) is not nested
	 * under a workflow.
	 */
	listAllSuggestions(options?: CallOptions): Promise<ResultOf<"/api/v1/suggestions", "get">>;
}
export declare class UrlsResource extends Resource {
	/** Paginated, filterable list of URL items. `pageSize` caps at 100. */
	list(query?: QueryOf<"/api/v1/urls", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls", "get">>;
	create(body: BodyOf<"/api/v1/urls", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls", "post">>;
	/** Create many URL items in one request — far cheaper than N calls. */
	createBatch(body: BodyOf<"/api/v1/urls/batch", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/batch", "post">>;
	/**
	 * Delete many URL items in one request.
	 *
	 * Note this is a `DELETE` **with a body** — unusual, but it is what the API
	 * defines, and the transport sends it. Like every write, it is never retried.
	 */
	deleteBatch(body: BodyOf<"/api/v1/urls/batch", "delete">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/batch", "delete">>;
	get(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/{id}", "get">>;
	update(id: string, body: BodyOf<"/api/v1/urls/{id}", "patch">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/{id}", "patch">>;
	delete(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/{id}", "delete">>;
	/** Enable or disable a URL item without deleting it. */
	toggle(id: string, body: BodyOf<"/api/v1/urls/{id}/toggle", "patch">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/{id}/toggle", "patch">>;
	/** Jobs run against this URL item. */
	listJobs(id: string, query?: QueryOf<"/api/v1/urls/{id}/jobs", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/{id}/jobs", "get">>;
	/** Extraction results for this URL item, newest first. */
	listExtractions(id: string, query?: QueryOf<"/api/v1/urls/{id}/extractions", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/urls/{id}/extractions", "get">>;
}
export declare class WebhooksResource extends Resource {
	/**
	 * Push URLs into the org's ingestion pipeline.
	 *
	 * Subject to a daily URL cap, which answers `429` with a flat message body —
	 * and, today, **no `Retry-After` header**. A retry is safe here only in the
	 * sense that ingestion dedupes; the SDK still does not retry it, because it is
	 * a POST.
	 */
	ingest(token: string, body: BodyOf<"/api/v1/webhooks/{token}/ingest", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/webhooks/{token}/ingest", "post">>;
}
/**
 * A workflow has two uuids, and every method here that takes an `id` wants the
 * SECOND one:
 *
 * - `workflowId` — the stable logical id, constant across versions. This is what
 *   every method below takes, and what jobs and schedules reference.
 * - `id` — the primary key of the version row you happen to be holding. It
 *   changes on every edit and no endpoint accepts it.
 *
 * The trap is that the useless one is the one called `id`, so the obvious line
 * is the broken one:
 *
 * ```ts
 * const [wf] = await jc.workflows.list();
 * await jc.workflows.get(wf.id);         // 404 — version row key
 * await jc.workflows.get(wf.workflowId); // correct
 * ```
 *
 * Both fields carry the distinction in their generated JSDoc, so hovering the
 * property in an editor says which is which.
 */
export declare class WorkflowsResource extends Resource {
	/**
	 * List workflows, optionally filtered by status or domain.
	 *
	 * Each item carries both `id` and `workflowId` — pass `workflowId` to every
	 * other method on this class. See the class doc above.
	 */
	list(query?: QueryOf<"/api/v1/workflows", "get">, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows", "get">>;
	/** Create a workflow. It starts unpublished — {@link publish} makes it routable. */
	create(body: BodyOf<"/api/v1/workflows", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows", "post">>;
	/** Create a pre-wired smart workflow rather than assembling the DAG by hand. */
	createDefaultSmart(body: BodyOf<"/api/v1/workflows/create-default-smart", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/create-default-smart", "post">>;
	/**
	 * One workflow, latest version, with its DAG.
	 *
	 * @param id The workflow's stable logical id (`workflowId`), not a list item's `id`.
	 */
	get(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}", "get">>;
	update(id: string, body: BodyOf<"/api/v1/workflows/{id}", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}", "put">>;
	/** Delete a workflow. Resolves to `undefined` — the API answers 204. */
	/**
	 * Soft-delete a workflow.
	 *
	 * @param id The workflow's stable logical id (`workflowId`), not a list item's `id`.
	 */
	delete(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}", "delete">>;
	/**
	 * Clone a workflow as a new non-smart draft.
	 *
	 * @param id The workflow's stable logical id (`workflowId`), not a list item's `id`.
	 */
	clone(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}/clone", "post">>;
	/**
	 * Check the DAG against the 17 publish-time legality rules without publishing.
	 *
	 * Worth calling before {@link publish} in any automated pipeline: publish
	 * enforces the same rules, but failing here costs nothing and names the rule.
	 */
	validate(id: string, body: BodyOf<"/api/v1/workflows/{id}/validate", "post">, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}/validate", "post">>;
	/**
	 * Make a workflow live. **Set its route first** — this is the one ordering
	 * mistake the API cannot forgive.
	 *
	 * `create()` hands back a workflow already routed to `'*'`, the org-wide
	 * default, and only one PUBLISHED workflow per org may hold a given route. So
	 * the naive sequence works exactly once per org and then starts failing:
	 *
	 * ```ts
	 * const wf = await jc.workflows.create({ name, dag });
	 * await jc.workflows.publish(wf.workflowId);   // 409 from the 2nd workflow on
	 * ```
	 *
	 * Give each workflow its own route before publishing:
	 *
	 * ```ts
	 * await jc.workflows.setRouting(wf.workflowId, { route: 'domain:example.com' });
	 * await jc.workflows.publish(wf.workflowId);
	 * ```
	 *
	 * Throws `JustCrawlError` with `status: 409` when another published workflow
	 * already holds the route, and `status: 400` when the DAG fails validation.
	 */
	publish(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}/publish", "post">>;
	unpublish(id: string, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}/unpublish", "post">>;
	/** Replace the domain→workflow routing table for this workflow. */
	setRouting(id: string, body: BodyOf<"/api/v1/workflows/{id}/routing", "put">, options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}/routing", "put">>;
	/**
	 * Fetch one historical version — jobs pin the version they ran against.
	 *
	 * `version` is the spec's `number`, not the `number | string` this accepted
	 * until the typed `request()` rejected the widening. The looser type promised
	 * a call the API does not serve.
	 */
	getVersion(id: PathParamsOf<"/api/v1/workflows/{id}/versions/{version}", "get">["id"], version: PathParamsOf<"/api/v1/workflows/{id}/versions/{version}", "get">["version"], options?: CallOptions): Promise<ResultOf<"/api/v1/workflows/{id}/versions/{version}", "get">>;
}
/**
 * The production API host — the spec's `servers` entry.
 *
 * Worth knowing why this is called out: until 2026-08-30 both this constant and
 * the spec said `https://dashboard.justcrawl.io`, which is the Cloudflare Pages
 * frontend. Its SPA fallback answers `/api/v1/*` with `index.html` at
 * `200 text/html`, so every call failed as a non-JSON 200 rather than an honest
 * 404 — the SDK was unusable at its default. Fixed at the source
 * (`generate-openapi.ts` + the route JSDoc) and verified end to end against
 * production, and corroborated by the deploy configuration, which uses
 * `api.justcrawl.io` for the analytics push URL, the WorkOS OAuth redirect, and
 * the share endpoints.
 */
export declare const DEFAULT_BASE_URL = "https://api.justcrawl.io";
export declare const DEFAULT_TIMEOUT_MS = 30000;
/** Minimal structural type for `fetch`, so injecting one needs no DOM lib. */
export type FetchLike = (input: string, init?: RequestInit) => Promise<Response>;
export interface JustCrawlOptions {
	/** An `sr_live_…` API key from Settings → API Keys. */
	apiKey: string;
	/** Defaults to `https://api.justcrawl.io`. Override for staging or self-hosted. */
	baseUrl?: string;
	/** Per-request timeout. Defaults to 30s. */
	timeoutMs?: number;
	/** Retries for GET requests only. Defaults to 2. */
	maxRetries?: number;
	/** Injectable `fetch` — every test in this package uses it, so none need a live API. */
	fetch?: FetchLike;
}
export interface RequestOptions {
	/** Query parameters. `undefined` values are dropped; arrays repeat the key. */
	query?: QueryParams;
	/** JSON request body. */
	body?: unknown;
	/**
	 * Values for the `{…}` placeholders in the path.
	 *
	 * Each is escaped with {@link encodePathParam} before substitution, so an id
	 * containing `/` or `?` cannot retarget the request at a different endpoint.
	 * The typed `request()` derives this object's shape from the spec; on
	 * `requestUnchecked()` it is free-form.
	 */
	path?: Record<string, string | number>;
	/** Caller cancellation, composed with the internal timeout signal. */
	signal?: AbortSignal;
	/** Override the client's timeout for this one call. */
	timeoutMs?: number;
	/** Extra headers. Cannot override `Authorization` — see `buildHeaders`. */
	headers?: Record<string, string>;
}
export type QueryValue = string | number | boolean | Date | undefined | null | Array<string | number | boolean>;
export type QueryParams = Record<string, QueryValue>;
/**
 * `RequestOptions` with `query` and `path` narrowed to the operation's own
 * parameters, so the typed `request()` rejects a filter the endpoint does not
 * accept and demands the path parameters it does.
 *
 * `path` is *conditionally* present: an endpoint with no `{…}` placeholder gets
 * `path?: never` (passing one is an error), and an endpoint with placeholders
 * gets them as a required object. That requiredness is why `request()`'s third
 * argument is a rest tuple rather than a plain `options?` — see the signature.
 */
export type TypedRequestOptions<P extends ApiPath, M extends MethodOf<P>> = Omit<RequestOptions, "query" | "path"> & ([
	PathParamsOf<P, M>
] extends [
	never
] ? {
	path?: never;
} : {
	path: PathParamsOf<P, M>;
}) & {
	query?: QueryOf<P, M>;
};
/**
 * `request()`'s trailing argument, optional only when the endpoint takes no path
 * parameters.
 *
 * Without this, `options` would be plain-optional and
 * `request('get', '/api/v1/jobs/{id}')` would compile — issuing a literal
 * request to `…/jobs/%7Bid%7D`. Making the tuple conditional is what turns a
 * forgotten id into a compile error rather than a 404 at runtime.
 */
export type RequestArgs<P extends ApiPath, M extends MethodOf<P>> = [
	PathParamsOf<P, M>
] extends [
	never
] ? [
	options?: TypedRequestOptions<P, M>
] : [
	options: TypedRequestOptions<P, M>
];
/** Masked stand-in used everywhere the key would otherwise be rendered. */
export declare const MASKED_API_KEY = "sr_live_****";
/**
 * The JustCrawl API client.
 *
 * ```ts
 * const jc = new JustCrawl({ apiKey: process.env.JUSTCRAWL_API_KEY! });
 * ```
 *
 * The `apiKey` is stored on a **non-enumerable** field and masked by both
 * `toJSON()` and Node's inspect hook, so it survives none of the usual accidental
 * exfiltration paths: `JSON.stringify(client)`, `console.log(client)`, a crash
 * reporter walking own properties, or a thrown error. It leaves the process only
 * as the `Authorization` header on a request to `baseUrl` (R10).
 */
export declare class JustCrawl {
	readonly baseUrl: string;
	readonly timeoutMs: number;
	readonly retryPolicy: RetryPolicy;
	/** One group per spec tag. See each group's module for what it covers. */
	readonly jobs: JobsResource;
	readonly workflows: WorkflowsResource;
	readonly smartWorkflows: SmartWorkflowsResource;
	readonly urls: UrlsResource;
	readonly schedules: SchedulesResource;
	readonly extraction: ExtractionResource;
	readonly analytics: AnalyticsResource;
	readonly benchmarks: BenchmarksResource;
	readonly plans: PlansResource;
	readonly integrations: IntegrationsResource;
	readonly webhooks: WebhooksResource;
	readonly bi: BiResource;
	/** Non-enumerable, assigned in the constructor. Never serialize this. */
	private readonly apiKey;
	private readonly fetchImpl;
	constructor(options: JustCrawlOptions);
	/** Masked view for `JSON.stringify`. */
	toJSON(): Record<string, unknown>;
	/**
	 * Issue one API request against a **documented** path — fully typed from the
	 * spec. An unknown path, a method the path does not define, or a missing path
	 * parameter is a compile error, and the return type is that operation's own
	 * success body.
	 *
	 * Paths are the spec's own templates, with values passed separately. That is
	 * the whole mechanism: `'/api/v1/jobs/{id}'` is a member of the `ApiPath`
	 * union and so is checked against the spec, whereas an interpolated
	 * `` `/api/v1/jobs/${id}` `` is just `string` and is checked against nothing.
	 *
	 * ```ts
	 * const account = await jc.request('get', '/api/v1/account');
	 * const job = await jc.request('get', '/api/v1/jobs/{id}', { path: { id } });
	 * ```
	 *
	 * For an endpoint this SDK version does not document, use
	 * {@link JustCrawl.requestUnchecked} — deliberately a separate method, because
	 * as a second overload of this one it silently accepted every typo'd path
	 * (see `src/__tests__/paths.test.ts` for the sweep that now guards this).
	 */
	request<P extends ApiPath, M extends MethodOf<P>>(method: M, path: P, ...args: RequestArgs<P, M>): Promise<ResultOf<P, M>>;
	/**
	 * Issue one API request against any path, documented or not — **unchecked**.
	 *
	 * The escape hatch, and deliberately an opt-in one. A preview endpoint, or one
	 * published after this SDK version was cut, still works; you supply the
	 * response type and accept that nothing validates the path.
	 *
	 * This used to be a second overload of {@link JustCrawl.request}. It could not
	 * stay one: an overload accepting `string` matches everything the
	 * literal-union overload would have rejected, so `request('get',
	 * '/api/v1/jobsss')` and `request('delete', '/api/v1/jobs/{id}')` both compiled
	 * clean. The type safety was advertised in the docs and absent in fact. Naming
	 * the unsafe form is what makes the safe form mean something.
	 *
	 * ```ts
	 * const preview = await jc.requestUnchecked<{ ok: boolean }>('get', '/api/v1/preview/thing');
	 * ```
	 */
	requestUnchecked<T = unknown>(method: string, path: string, options?: RequestOptions): Promise<T>;
	/**
	 * The single implementation behind both public forms — every resource wrapper
	 * and every escape-hatch call funnels through here, so auth, retries,
	 * timeouts, and error mapping have exactly one home (R6).
	 *
	 * @returns The parsed JSON body, or `undefined` for a 204 / empty 2xx.
	 * @throws {JustCrawlError} on any non-2xx, timeout, or transport failure.
	 */
	private send;
	/**
	 * One attempt: build headers, arm the timeout, call `fetch`, **and read the
	 * body** — all inside the same armed window.
	 *
	 * Reading the body here rather than in `request()` is the whole point. A
	 * `fetch` promise settles when the response HEADERS arrive; the body is still
	 * streaming. If the timer were cleared and the caller's abort listener removed
	 * at that moment, every body read would run with no timeout and no
	 * cancellation — a server that sent headers and then stalled would hang the
	 * caller forever, and `timeoutMs` would silently mean "time to first byte"
	 * rather than "time to a usable result".
	 *
	 * It also keeps mid-body failures inside the `catch` below, so a connection
	 * reset during the body surfaces as a typed `JustCrawlTimeoutError` /
	 * `JustCrawlAbortError` / `JustCrawlConnectionError` like every other
	 * transport failure — and, on a GET, is retryable for the same reason.
	 */
	private dispatch;
	/**
	 * Compose request headers.
	 *
	 * The SDK's own headers are applied **last**, so a caller-supplied header can
	 * never displace them — silently swapping the key would send the request
	 * unauthenticated, or worse, with someone else's credential.
	 *
	 * "Applied last" is necessary but NOT sufficient, which is the subtle part.
	 * HTTP header names are case-insensitive but **object keys are not**: a caller
	 * passing `authorization` (lowercase) survives the spread as a second, distinct
	 * key alongside our `Authorization`, and `fetch`'s `Headers` constructor then
	 * COMBINES same-named values rather than letting the later one win:
	 *
	 *   { ...{ authorization: 'X' }, Authorization: 'Bearer k' }
	 *     -> outgoing header: `authorization: X, Bearer k`
	 *
	 * So the caller's value is sent, the key is smuggled alongside it, and the
	 * request fails authentication in a way that reads like a bad key. Reserved
	 * names are therefore filtered out of the caller's map by CASE-FOLDED
	 * comparison before ours are applied.
	 */
	private buildHeaders;
	/**
	 * Read a successful response body.
	 *
	 * A 204 or any zero-length 2xx resolves to `undefined` rather than throwing a
	 * JSON parse error — five wrapped DELETE endpoints return 204.
	 */
	private parseBody;
}

export {};
